# Adversarial review: tsnet lifecycle at the Aperture app layer
# (kimi-k3, via cross-LLM review — content recovered from the
#  session thinking block; kimi-k3 + --print emitted no text answer.)

I need to write the full review now. Let me synthesize everything I found from the files I read:

Key facts gathered:

1. **`timing/README.md`**: Library phases are all seconds/sub-seconds. Two suspected app-layer culprits: 60s `watch-ipn-bus` inactivity timeout; `TailscaleNode.up()` blocking the actor so `close()` deadlocks.

2. **`TSNet/TSNetManager.swift`**:
   - `startTailscaleIfNeeded` with `startInFlight` @MainActor guard. `init()` calls it. `startTailscale()` is `nonisolated` async: `setupNode()` via `MainActor.run`, creates LocalAPIClient, `setLocalAPIClient`, then `tailscaleUp` → `startEventBus` → `setProcessor` → `node?.up()` → `loopback()` → sets `proxyConfiguration`.
   - **CRITICAL: on `catch`, it does `fatalError`!** `startTailscale()` catch: `await MainActor.run { startInFlight = false }` then `fatalError("Error setting up Tailscale: \(error)")`. So ANY error in setup — including `tailscale_up` failing (network down, control unreachable) — crashes the app. Wait — `try await node?.up()` — if up() throws (e.g., the node was closed by `willEnterBackground` mid-up — actually close would queue behind up... hmm, up() error e.g. when close() is called concurrently in Go: `TsnetClose` while `TsnetUp` is blocked → `Up()` returns error "server closed" or similar → fatalError → crash. Actually backgrounding during `up()` — `willEnterBackground` spawns `Task { try await nodeTmp?.close() }`. close() is actor-isolated on TailscaleNode; up() is running on the actor. up() blocks the actor until Up returns. close() queues behind up(). When does up() return? `s.s.Up(context.Background())` — when the node reaches Running OR fails. If the node is at NeedsLogin (no auth key), does Up() block? Per the README: "up() calls Go's Up(context.Background()), which is non-cancellable and blocks the TailscaleNode actor until Running; close() is on the same actor and would queue behind it forever for a no-key node stuck at NeedsLogin (a real deadlock)". So for a fresh node (no auth key), `tailscale_up` blocks until Running — which only happens after the user completes web auth! So the startTailscale task sits at `try await node?.up()` for potentially minutes/hours. That means `startInFlight` stays `true` until up() returns... and `setProcessor` etc. happened before up(). That's mostly fine for the UI (bus watcher is already attached before up()), but:
     - `model.proxyConfiguration` is only set AFTER `up()` returns and `loopback()` succeeds. For a no-key node sitting at NeedsLogin, up() never returns → proxyConfiguration is nil until login completes. That's OK because loads are held until connected anyway. But wait — the localAPIClient requests go through `URLSessionConfiguration.tailscaleSession(node)` → `node.loopback()` — that's called in `basicAuthURLRequest` per request, so localAPI works before up() returns. OK.
     - **DEADLOCK on backgrounding while at NeedsLogin**: `willEnterBackground` → `Task { try await nodeTmp?.close() }` → close() queues on the TailscaleNode actor behind the blocked up() → close never runs. But worse: the background task that's awaiting close() suspends forever. iOS gives the app ~seconds to finish backgrounding; the close never completes, so the Go node stays alive with its sockets... Actually the process gets suspended. On foreground, `willEnterForeground` → `startTailscaleIfNeeded` — `startInFlight` was set false in willEnterBackground, so it starts a NEW start: `setupNode()` — `self.node` was nil'd in willEnterBackground, so it creates a NEW TailscaleNode with the SAME state dir (`tailscale_set_dir`). Meanwhile the OLD TailscaleNode actor still exists (retained by the deadlocked close() task), still blocked in up(). Two tsnet servers on the same state dir! Hmm — actually would `tailscale_start` on the second handle succeed? In Go, `tailscale_start` creates a new tsnet server with the same dir. The old one is still running (blocked in Up waiting for Running... actually Up at NeedsLogin — does Up return error when NeedsLogin? Per README it blocks until Running). Two servers same state dir → state file corruption risk, both fighting. Also the new node's up() blocks too.
     - Also on foreground re-entry after the close-deadlock: the old node never closed, and `self.node` points to the new node; the old one leaks.
   - `startInFlight` set to `false` in `willEnterBackground` — but the previous `startTailscale()` Task may still be running (blocked in `up()` on the actor!). Then foreground → new `startTailscale()` → `setupNode()` returns... `self.node` was nil'd, so new node. But wait, there's a subtler race: `willEnterBackground` runs on MainActor; `startTailscale` is nonisolated, `setupNode()` hops to MainActor via `await MainActor.run { try setupNode() }`. Sequence: foreground event 1 → startTailscaleIfNeeded (startInFlight=true) → Task begins startTailscale → before it reaches `MainActor.run { setupNode() }`, background event → startInFlight=false, node=nil (it's already nil), close task on nil → no-op. Foreground event 2 → startTailscaleIfNeeded → startInFlight=true → second startTailscale task. Now BOTH tasks race to `MainActor.run { setupNode() }`. First one creates node; second one gets the same node back (guard self.node == nil else return self.node). So they share a node — then both proceed: two LocalAPIClients (fine), `setLocalAPIClient` twice (second wins, fine), two `tailscaleUp` calls → `startEventBus` twice → two MessageProcessors (first gets cancelled by setProcessor — `setProcessor` cancels the old one. OK). Then both call `node?.up()` — TWO `up()` calls on the same actor → they serialize: first up() blocks actor until Running; second up() queues behind it. After first returns at Running, second runs `tailscale_up` again — Up on an already-running server returns quickly (it's idempotent-ish, blocks until Running which is already true). Then both set proxyConfiguration. So mostly benign but two up()s, and if the first up() fails with error → fatalError → crash. And the check-and-set race: is it actually atomic? `startTailscaleIfNeeded` is @MainActor — the guard and set are atomic on MainActor. The init-triggered start and the scenePhase .active notification: init runs `startTailscaleIfNeeded()` synchronously (MainActor, sets startInFlight=true). Later, scenePhase .active → `willEnterForeground` → `startTailscaleIfNeeded` → guard trips → skipped. So the guard works for that. BUT: the double-start CAN still happen if `willEnterBackground` fires between init and the .active (inactive → background transitions): e.g. launch → init (startInFlight=true) → scenePhase .inactive → `willEnterBackground` (startInFlight=false, node=nil, close(nil)) → scenePhase .active → `willEnterForeground` → start #2. Now start #1's task and start #2's task race as above. Not fatal but messy. More importantly: **scenePhase .inactive triggers willEnterBackground**! In ApertureApp: `case .background, .inactive: workspaceManager.willEnterBackground()`. `.inactive` fires when: Control Center is pulled down, a phone call arrives, the app switcher is invoked, an ASWebAuthenticationSession is presented?? Actually presenting an ASWebAuthenticationSession — does that make the scene inactive? The auth session runs in-process (SFAuthenticationSession-like) with a system UI. On iOS, presenting ASWebAuthenticationSession does NOT resign active (the window stays key). Hmm, but the system auth alert... I believe the scene stays active. However, app switcher / control center → .inactive → **tears down the node**. That's aggressive: swiping up to the app switcher closes the tailscale node, then swiping back re-creates it. And during login: user taps Login → sheet opens; if any notification banner or control center gesture happens → node torn down mid-login. Also on iPad multitasking. This is a real problem: .inactive ≠ background. HIGH finding: `.inactive` tears down the node, and immediately `.active` restarts — each cycle kills and recreates the node; if it happens during up()-blocked (NeedsLogin), we get the leaked-node/two-node situation and the close deadlock.
   
   - `startEventBus` restart logic: 
     ```swift
     let busObserver = await consumer.$error
         .sink { [weak self] error in
             guard error != nil else { return }
             logger.log("Restarting bus watcher")
             Task { [weak self] in
                 guard let self else { return }
                 await MainActor.run { consumer.error = nil }
                 let processor = try await startEventBus(localAPI: localAPI, consumer: consumer)
                 await MainActor.run { self.setProcessor(processor) }
             }
         }
     ```
     Problems:
     a) `try await startEventBus(...)` inside a `Task` that doesn't catch — if `startEventBus` throws (e.g., localAPI unreachable because node is mid-restart / loopback not ready), the Task silently fails (unhandled error in Task → just dropped since Task not awaited and error not handled... actually `Task { try await ... }` — the task returns a throwing result that's never observed; error silently swallowed). So the bus watcher is NEVER restarted and there's no further error to trigger another restart — **the consumer.error was cleared first, the old watcher is dead, and no new watcher exists. The app now has NO bus observation forever — state never updates again.** This is a FATAL/permanent-hang path. When does `watchIPNBus` throw? `basicAuthURLRequest` → `tailscaleSession(node)` → `node.loopback()` → `tailscale_loopback` — throws if the node is closed/nil handle... Also the `await consumer.$error` — `$error` is a `@Published` on the actor with `@MainActor @Published var error`. Wait — `TSNetConsumer` is an `actor`, but `error` is declared `@MainActor @Published var error: Error? = nil`. And `consumer.$error` — accessing a @MainActor property of an actor... The `sink` closure: `[weak self] error in ... Task { ... }` — the sink runs wherever $error publishes. `@Published` publishes on the actor it's isolated to — @MainActor here, so sink body runs on MainActor? Combine's Published sends values on the publisher's... for an @MainActor-isolated @Published, the value is sent on the main thread. The sink body is nonisolated (closure) — it can run on main thread. It spawns a Task (MainActor-inherited since TSNetManager is @MainActor and startEventBus is called from... wait, `startEventBus` in TSNetManager — the class is @MainActor so `startEventBus` is @MainActor-isolated unless marked nonisolated. It's not marked nonisolated, so `startEventBus` is @MainActor. `let processor = try await localAPI.watchIPNBus(...)` — fine. `await consumer.$error` — hmm, accessing `consumer.$error` from MainActor: `error` is @MainActor-isolated member of the actor, so sync access OK.
     b) **The restart gap**: between the old watcher dying (didCompleteWithError → errorHandler → consumer.error set via Task {@MainActor}) and the new watcher's HTTP request actually reaching the Go backend, notifies are dropped. The new watcher has mask `[.initialState]` and per the Go source I read: `if mask&ipn.NotifyInitialState != 0 { ini.State = ...; if b.state == ipn.NeedsLogin && b.authURL != "" { ini.BrowseToURL = new(b.authURL) } }`. **Good news**: the initial dump DOES include BrowseToURL when state == NeedsLogin and authURL is set. So a login URL emitted during the gap is recovered by the restart's initial dump (as long as state is NeedsLogin). BUT the mask is ONLY `.initialState` — no `.initialPrefs`, no `.initialNetMap`. So Prefs and NetMap updates emitted during the gap are LOST until the next change. `model.netmap`/`prefs` go stale — e.g. exit node ID, hostname, identity — until something else changes. Not fatal but a real staleness bug. Also, what about the rate of restarts — every ~60s the watch dies with -1001 (URLRequest default timeoutInterval = 60s, since `watchIPNBus` doesn't set `request.timeoutInterval`; doSimpleAPIRequest sets 60s for simple requests but watchIPNBus builds its own request via basicAuthURLRequest and never sets timeoutInterval → default 60s). So the watcher dies every 60s if no notify traffic. Each death → error → restart. Each restart re-sends initial State. That's a constant 60s-cycle of churn. The restart also happens through this fragile path each time. And each restart's initial dump re-emits `State` — e.g. re-emitting NeedsLogin every 60s... wait, `removeDuplicates` on state filters that for observeAuthURL. But TSNetConsumer sets model.state each time (published), removeDuplicates in the VM dedups. OK.
     
     Actually wait — one more subtle thing on the restart: `startEventBus` creates a NEW `MessageProcessor` each time and the old one is only cancelled via `setProcessor` AFTER the new watchIPNBus returns. The old MessageProcessor's `pollTask` is still running, its `MessageReader` already errored. `setProcessor` cancels old → `pollTask?.cancel()` — but `reader.stop()` is only called in deinit! `cancel()` doesn't stop the URLSession/reader — `deinit` calls `reader.stop()`. If processor is retained... it's replaced, old one deinits (assuming no other retain — the sink closure? `busErrorWatcher` retains the AnyCancellable whose closure captures `localAPI` (the client) not the processor. OK). Actually `MessageProcessor.cancel()` only cancels pollTask; the reader's URLSession data task already completed with error, so it's dead anyway. Fine.
     
     c) The `consumer.error = nil` clear happens BEFORE the new watch starts; if `startEventBus` throws, error is nil and nobody retries — the permanent-no-watcher state above. Also, the error that triggered the restart might fire MULTIPLE times? $error publishes distinct... `consumer.error` is set via `Task { @MainActor in self.error = error }` from `error(_ error:)` — @Published fires for each set even if non-nil already (Published doesn't dedup). Two errors (e.g., timeout then connection-reset) → two sink firings → two restart Tasks → two watchIPNBus → two processors; setProcessor cancels the first, second survives. Also `busErrorWatcher` gets REASSIGNED in the restarted `startEventBus` (the recursive call re-runs `await MainActor.run { busErrorWatcher = busObserver }`) — replacing the old AnyCancellable — fine-ish, the old sink is cancelled when replaced (AnyCancellable cancels on deinit) — but wait: the OLD sink's Task is what's RUNNING this code! The recursive startEventBus call reassigns `busErrorWatcher` → old AnyCancellable deinits → cancels the old subscription. The old subscription is the one whose closure spawned this very Task. Fine — the Task continues. But: is there a race where the new sink misses an error that was set between the old sink's cancellation and the new sink's attachment? `consumer.error = nil` then new watch attaches a new sink to `consumer.$error`. If `consumer.error(...)` fires between old-sink-cancel and new-sink-attach... the new sink attaches inside the new startEventBus BEFORE watchIPNBus? No: `let processor = try await localAPI.watchIPNBus(...)` happens FIRST, THEN `consumer.$error.sink`. Hmm wait no, let me re-read:

     ```swift
     func startEventBus(...) async throws -> MessageProcessor {
         let processor = try await localAPI.watchIPNBus(mask: busEventMask, consumer: consumer)
         let busObserver = await consumer.$error.sink { ... }
         await MainActor.run { busErrorWatcher = busObserver }
         return processor
     }
     ```
     The watcher starts (watchIPNBus → messageProcessor.start immediately — the URLSession data task resumes NOW), and the error sink attaches AFTER the await. If the watch request fails FAST (e.g., connection refused because Go loopback isn't up yet) — `didCompleteWithError` → processError → `Task { await consumer.error(error) }` → `Task { @MainActor in self.error = error }` — this can land BEFORE the `.sink` is attached? `$error` is a CurrentValue-like? No — `@Published` projected value replays the CURRENT value to new subscribers! `Published.Publisher` sends the current value upon subscription. YES — @Published's publisher replays the current value on subscribe. So a late-attaching sink immediately receives the current `error` value. That's why `guard error != nil else { return }` exists — the first emission is the current nil. So if error was set before sink attach, the new sink fires immediately with the non-nil error → triggers ANOTHER restart. OK so that's actually robust against that race... but creates a RESTART LOOP risk: error set → sink fires → Task clears error, restarts; if watchIPNBus keeps failing (node down), each startEventBus's sink attach sees... error was cleared to nil first, then if the new watch fails, error gets set again → restart → fail → restart — infinite tight restart loop while the node is down/unreachable! Each cycle logs "Restarting bus watcher" and spawns URLSessions. And actually, hmm, wait: if `watchIPNBus` throws synchronously (loopback fails), the Task dies silently (point a above) — no loop, just dead. If watchIPNBus succeeds but the request fails async → error → loop. Depends on failure mode.

     d) Another BIG one: **the restart path never re-checks whether the node even still exists**. `willEnterBackground` cancels `busErrorWatcher` — good. But a restart Task already in flight isn't cancelled; it'll call watchIPNBus on the OLD localAPIClient bound to the OLD node after backgrounding... The Task isn't tracked/cancelled on background. It can restart the bus on a closed node → throws → swallowed. Minor.

     e) **The -1001 timeout**: confirmed `watchIPNBus` request uses `URLRequest(url:)` default `timeoutInterval` = 60s (never overridden; `basicAuthURLRequest` doesn't set it; only `doSimpleAPIRequest` sets `request.timeoutInterval = timeoutInterval` and watchIPNBus doesn't go through doSimpleAPIRequest). URLSession request timeout semantics: if no data received for timeoutInterval → -1001. During idle (no notifies), every 60s the watch dies → restart cycle. Per the Go code, there IS no keep-alive on watch-ipn-bus (sender.Run(ctx, ch) only sends notifies; NotifyWatchEngineUpdates would add 2s polling but mask doesn't include it). So yes — the bus churns every 60s. Now — is the "UI sits un-updated for up to 60s" claim right? Partially: when a state change happens mid-window... no wait — the watch is a live HTTP stream; when a notify IS sent during a healthy window, it's delivered immediately. The 60s timeout only bites when the watch has ALREADY died and is awaiting restart (gap of maybe tens/hundreds of ms) OR when the restart silently failed (point a — then it's FOREVER, not 60s). So the 60s timeout itself isn't "UI frozen for 60s per cycle" — the README slightly overstates that. The real killer is the SILENT restart failure (unhandled throw in the restart Task) → no watcher forever → the only remaining signal is... nothing — `state` is purely bus-driven. `startStatusPolling` polls `backendStatus()` every 5s but only publishes `model.localStatus` (IpnState.Status), which feeds `ConnectionTypeResolver` for tab indicators — NOT `model.state`, not `running`, not `needsAuth`. So if the bus watcher dies permanently, Login/Logout buttons appear to do NOTHING forever (state never flips), even though the library did the work in 300ms. THIS is the "minutes" mechanism: user clicks Logout → deleteProfile succeeds in 300ms → but if the watcher was dead (silently failed restart, or gap), the NeedsLogin notify is never observed → UI shows Connected forever → user clicks again, nothing → force-quit. Actually wait — if the watcher is merely in a restart gap (~sub-second), the initialState resend catches up. The permanent-dead case requires startEventBus to throw. When would watchIPNBus throw while the node is alive? `node.loopback()` throws if `tailscale_loopback` fails... after a background/foreground cycle, `localAPIClient` is rebound; the RESTART closure captures the OLD `localAPI` client and OLD node — after foreground, restarts triggered by old-consumer errors would recreate a watcher via the OLD client on the OLD (closed) node → `watchIPNBus` → old node's loopback → the old TailscaleNode actor... the actor still exists (if not deinited), its tailscale handle was closed → `tailscale_loopback` on closed handle → error → throw → swallowed → dead watcher. But note: after foreground, a FRESH startEventBus runs (from tailscaleUp) with the NEW client, and setProcessor replaces. The busErrorWatcher also gets replaced by the fresh one. So the old restart closure is dead then. OK.

     But here's a much more common silent-death: **60s timeout fires while app sits at the connection gate in NeedsLogin** (idle — no notifies for 60s+). Watch dies → error → restart Task → `consumer.error = nil` → `startEventBus` → watchIPNBus on the live node → succeeds → new watcher with initialState → re-sends State=NeedsLogin (+ BrowseToURL since NeedsLogin & authURL set — good). So the login URL is re-delivered each restart. Hmm, so at the gate, the cycle is self-healing. BUT: `observeAuthURL`'s stale-URL clearing: on NeedsLogin branch it sets authURL from browseToURL. Each 60s cycle re-emits NeedsLogin + same URL. removeDuplicates on state: state goes NeedsLogin → (no change emitted, dedup) → combineLatest still fires when browseToURL republishes (same URL string — @Published doesn't dedup; combineLatest fires on any new value from either). authURL reassigned same value; needsAuth stays true. `requestedInteractiveLogin` false → no sheet reopen. OK.

     **THE key login hang**: user taps Login → `showAuth()`: if authURL != nil → `authManager.showAuth(authURL:)` opens ASWebAuthenticationSession with the URL. Sheet opens with the ORIGINAL login URL (emitted at node start — could be minutes old). Tailscale login URLs... do they expire? The comment in observeAuthURL says a stale URL completes OAuth on the control plane but the node "isn't watching for that callback" → relogin silently fails. The recent fix clears stale browseToURL in the non-NeedsLogin branch. But at FIRST login (never connected), the URL isn't stale in that sense — the node IS at NeedsLogin watching. Hmm, but consider: **user taps Login, the sheet opens; mid-auth the bus watcher does its 60s restart; nothing bad.** Consider instead: user taps Login → sheet opens → user backgrounds the app (or the OS shows a call/Control Center → `.inactive` → willEnterBackground!) → node closed → proxyConfiguration nil'd → foreground → node restarts → NeedsLogin + NEW URL — but the ASWebAuthenticationSession is still open with the OLD URL (AuthManager.cancel is only called when state leaves NeedsLogin; after restart state goes ... hmm, model.state — on background, model.state is NOT reset! willEnterBackground doesn't clear model.state. The new node's initial dump sends State (Starting → NeedsLogin w/ new URL). State transitions NeedsLogin→(new watch initial)NeedsLogin — removeDuplicates means the VM sink might not re-fire for state, but browseToURL changes → combineLatest fires → NeedsLogin branch → authURL = new URL; `requestedInteractiveLogin` is false → sheet NOT updated. The old sheet is still presenting the old URL → user completes auth on the old URL → control plane says OK but the node (new instance) isn't watching that callback → silent failure — exactly the stale-URL bug the fix was supposed to address, still reachable via background-during-login or inactive-during-login. And `.inactive` (Control Center, app switcher peek) triggers it without even leaving the app! HIGH.

   - `up()` blocking + `close()` deadlock — the README flags it; confirm app-layer impact: app at gate (NeedsLogin, no auth key) → user locks phone / app switcher → willEnterBackground → close() queued behind up() forever → the close Task suspends forever. iOS suspension arrives; the Go runtime threads get frozen with sockets open. Foreground → new node created on same state dir while old node still alive in-process. Old node's `tailscale_close` NEVER runs (deadlock). Two tsnet servers sharing one state dir → the Go side: does tailscale_start on the same dir fail ("state file locked")? tsnet uses a state store file; two servers writing → corrupts or the second fails to start → setupNode throws → startTailscale catch → **fatalError → CRASH**. So backgrounding at the login gate can crash or wedge the app on return. Even if no crash, the user sees "Connecting…" forever (gate) because... the new node may work and reach NeedsLogin — but the close task hangs forever holding... actually also `nodeTmp` retains old node, up() still blocked → if user then logs in via the NEW node... the OLD node also transitions? The old node never got credentials... it sits at NeedsLogin forever, up() blocked forever, close queued forever. Leaked actor + goroutine. Next background cycle: nodeTmp = new node, close on new node — new node's up() also blocked (if still NeedsLogin) → another deadlocked close. Each bg cycle at the gate leaks a node. And each foreground creates a new node on the same state dir → eventually tailscale_start fails or the state dir has N servers. FATAL-adjacent. Actually — WAIT. Does `up()` really block at NeedsLogin? The README says so (harness phase 1 avoided up() for exactly this reason: "for a no-key node stuck at NeedsLogin"). And TsnetUp: `s.s.Up(context.Background())` — ipnlocal Up() blocks until WantRunning && ... it waits for the backend to reach Running or return error. At NeedsLogin, Up blocks. Confirmed by harness design. So yes.

   Hmm, but hold on — in the app flow, `try await node?.up()` — for the app at the gate (no auth key), up() blocks on the actor. Meanwhile `tailscaleUp` also already did startEventBus + setProcessor BEFORE up(). LocalAPIClient requests (loopback()) — `loopback()` is... declared `public func loopback() throws -> LoopbackConfig` — is it actor-isolated? TailscaleNode is an actor; `loopback()` is NOT marked nonisolated → actor-isolated → **every localAPI request's `basicAuthURLRequest` → `tailscaleSession(node)` → `node.loopback()` blocks behind the blocked up()!** Wait — that would mean NO localAPI calls work while up() is blocked — but the app at the gate DOES make localAPI calls: `startLoginInteractive()` when Login tapped (in the no-authURL path), `currentProfile`/`deleteProfile` on logout, `backendStatus` polling every 5s, and `watchIPNBus`... wait but watchIPNBus worked in the harness — the harness uses Start (via init) not up(). Hmm, but `loopback()` caches `loopbackConfig` — first call does `tailscale_loopback`... Every call to `LocalAPIClient.basicAuthURLRequest` calls `await node.loopback()` — a cross-actor call → if up() is executing (synchronously blocked in `tailscale_up` — the actor's executor is a serial executor; the synchronous C call `tailscale_up` runs ON the actor's executor thread and BLOCKS it until it returns!) — **while `tailscale_up` is blocked (which for a gate-sitting node is INDEFINITELY), the TailscaleNode actor's serial executor is occupied → EVERY other actor-isolated call (loopback(), close(), addrs(), crashTest) queues forever.** So: `backendStatus()` poll → basicAuthURLRequest → `try await URLSessionConfiguration.tailscaleSession(node)` → `await node.loopback()` → HANGS. `startLoginInteractive` (the showAuth else-branch) → HANGS. `logout()` → currentProfile → HANGS. **The entire localAPI freezes while up() is blocked at NeedsLogin!** 

    Wait — is that right? `up()` is `public func up() async throws` — actor-isolated, and it makes a synchronous blocking C call. Swift actor methods run on the actor's executor; an async method that makes a sync blocking call occupies the executor thread until the method hits a suspension point or returns. `up()` has NO suspension points — `tailscale_up` blocks the thread. So the actor executor is blocked for the whole duration. YES — this is the FATAL finding. At the login gate (no auth key), from the moment `tailscaleUp` calls `node?.up()`, the node actor is frozen until login completes (state Running). During that window:
    - `startStatusPolling` → backendStatus → hangs (first poll reaches loopback() and queues). Each 5s poll spawns more queued calls? No — the poll loop is sequential: `try? await client.backendStatus()` hangs → loop stuck. Fine, just stuck.
    - `showAuth()` else-branch (`startLoginInteractive`) → hangs → if `authURL` was nil (e.g., cleared by the new stale-URL fix and no fresh URL yet — exactly the post-logout state!), tapping Login does NOTHING. The Task awaits forever. The sheet never opens. THE POST-LOGOUT LOGIN IS DEAD until up() returns — and up() returns only at Running, which requires login, which requires the sheet... which requires localAPI... **CIRCULAR DEADLOCK** unless a BrowseToURL arrives via the bus with `requestedInteractiveLogin` set — but `requestedInteractiveLogin` was set, and when the fresh BrowseToURL arrives (does it arrive without startLoginInteractive? After deleteProfile the node goes to NeedsLogin — does the backend emit a BrowseToURL on its own? Per the code comment: "after a logout — when the node returns to NeedsLogin WITHOUT emitting a fresh URL". So NO fresh URL → the sheet can never open post-logout. Hmm wait — but the bus watcher's initialState dump includes BrowseToURL if `state == NeedsLogin && authURL != ""`. Is b.authURL still set after logout-to-NeedsLogin? After deleteProfile, the profile is deleted; backend state → NeedsLogin; authURL empty (no interactive login started). So initial dump has no BrowseToURL. So post-logout: authURL=nil (fix cleared it) → Login tap → startLoginInteractive → **hangs on the frozen actor** → no URL ever → Login button permanently dead → "I clicked Login and nothing happened for minutes." THAT'S THE BUG. It self-resolves only when up() returns — which is at Running — impossible — OR when up() errors. Actually hmm, wait: does `Up()` in tsnet return when the state becomes NeedsLogin? Let me think again... In tailscale ipnlocal, `Up()` (via `Start()` + wait): tsnet's `Server.Up(ctx)` does: `s.Start(ctx)` then `lc.WaitStatus(ctx, ipn.Running)`... Actually tsnet Server.Up: starts localbackend and blocks until Running or error; if login is needed it... I recall tsnet.Up returns an error or blocks? The README says it blocks until Running ("blocks the TailscaleNode actor until Running") — confirmed empirically by the harness: phase 1 measured Start→URL not Up→URL because Up blocks for a no-key node. OK so up() blocks until Running. Confirmed.

    Hmm wait, actually — but then how did the FIRST login ever work in this app? At first login: node starts (init → tailscale_start → Start() begins, doInit sets WantRunning + StartLoginInteractive per README → bus emits NeedsLogin + BrowseToURL on its own) → TSNetConsumer sets browseToURL → VM: authURL != nil → Login tap → showAuth with authURL → sheet opens (NO localAPI call needed!). User auths → node reaches Running → up() returns. So first login works WITHOUT touching the frozen actor. The post-LOGOUT login is the broken one: after deleteProfile, no fresh URL, authURL nil → showAuth falls to startLoginInteractive → hangs forever behind up(). And up() is still blocked from the ORIGINAL start (it never returned — the node reached Running, so up() DID return at first Running!). Wait — sequence: launch → up() blocks → first login → Running → up() RETURNS → actor free → proxyConfiguration set. Then logout → deleteProfile (localAPI works, actor free) → NeedsLogin. Is up() re-called after logout? NO — nothing calls up() again. So after the first Running, up() has returned and the actor is FREE. So post-logout, startLoginInteractive... does NOT hang?? Hmm. But wait — deleteProfile on tsnet: what happens to the backend? The harness phase 4 shows Logout→idle in 300ms via currentProfile+deleteProfile. After deleteProfile, state → NeedsLogin (or Stopped?). Harness says "Logout → idle (NeedsLogin/Stopped/NoState)". And WantRunning? deleteProfile likely leaves the node stopped/needs-login. The actor is free. startLoginInteractive → POST login-interactive → localAPI → backend emits NEW BrowseToURL → bus → sheet opens. That should work in ~seconds... UNLESS the bus watcher is mid-restart gap or dead. OK so where do the MINUTES come from?

    Let me re-enumerate the minute-scale hang mechanisms:
    1. **Background at the gate (before first Running)**: up() blocked → close() deadlocks → old node never closes → foreground creates second node on same state dir → possible crash (fatalError) or wedge. Also the .inactive→willEnterBackground mapping means app-switcher/Control Center at the gate does this. Also foreground restart while up() of old node blocked: new setupNode... `self.node` was nil'd → new TailscaleNode init → `tailscale_start` — two servers, same dir. Go tsnet state file: tsnet uses a single state file (tailscaled.state in the dir) — two servers will clobber each other; the second `tailscale_start`... might succeed (no lock). Both try to bind the same loopback port? Loopback is per-server random port. Both connect to control with the same node key — the control plane sees flapping. The NEW node emits NeedsLogin+URL; user logs in; old node still blocked. Messy but may limp along. The real killer: **close() deadlocked forever → the close Task never completes → but iOS doesn't wait for unstructured Tasks before suspending** — the app just suspends with the node running. OK.
    2. **up() blocking the actor freezes ALL localAPI** during the initial pre-first-Running window — including `logout()` (can't happen, not logged in), `backendStatus` polls (harmless), and `startLoginInteractive` (needed only if authURL nil at the gate — e.g. URL dropped in a bus restart gap... but initialState resend re-delivers it since state==NeedsLogin&&authURL!=""). Actually wait — CAN authURL be nil at the gate? The new fix clears browseToURL in the non-NeedsLogin branch. At the gate: node start → initial dump: State=Starting (or NoState) with NO URL → non-NeedsLogin branch → clears browseToURL (nil already), then State=NeedsLogin+BrowseToURL → authURL set. Then each 60s bus restart re-sends NeedsLogin+URL. What if a restart delivers State=NeedsLogin via initial dump WITHOUT BrowseToURL? That happens if `b.authURL == ""` at that moment — e.g., right after startLoginInteractive... no, that SETS authURL. After Start at launch, authURL is set (doInit does StartLoginInteractive per README — "Start()'s doInit already sets WantRunning + calls StartLoginInteractive"). OK so URL persists. But combineLatest mis-ordering (below) can leave authURL nil transiently.
    3. **The silent restart failure** (unhandled throw) → bus dead forever → UI state frozen forever → Login/Logout appear dead
