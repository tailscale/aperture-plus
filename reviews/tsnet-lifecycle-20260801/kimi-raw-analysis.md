Let me start by exploring the repository structure to understand what we're working with.

--- ANALYSIS CHUNK ---

Let me start by reading the key files. First, the recent commits from 5071bac through HEAD to understand what changed.

--- ANALYSIS CHUNK ---

This is a substantial review task. Let me read all the key files now. Let me read the rest of the TSNet files.

--- ANALYSIS CHUNK ---

Now let me read the Browser files.

--- ANALYSIS CHUNK ---

Now let me read the tailscale.go, tailscale.h to check the C→Go call semantics, especially `tailscale_debug_reset_connections`, `tailscale_close`, `tailscale_start`, `tailscale_loopback`, `tailscale_up`.

--- ANALYSIS CHUNK ---

Now let me check the `DebugAction` semantics in the pinned tailscale module. Let me check what "rebind" and "break-derp-conns" do in the vendored tailscale.com code.

--- ANALYSIS CHUNK ---

Now, let me check what `s.s.Start()` does — specifically whether it calls `StartLoginInteractive` as claimed in the TSNetManager comment. Let me look at doInit in tsnet.go.

--- ANALYSIS CHUNK ---

Good. Now let me check the key claim in the code: "TailscaleNode is an actor... `close()` on background". Let me check the history of the repo to understand what the original design was, and then check the tests.

Let me look at the remaining app files: StatusViewModel, BrowserView, RawWebView, ConnectionGateView, and the UITests + test-lock-resume.sh. Also check TailscaleKit URLSession+Tailscale.swift.

--- ANALYSIS CHUNK ---

Now let me look at the UITests and the test-lock-resume.sh script:

--- ANALYSIS CHUNK ---

Now let me look at the ProxyBounceTestHarness, and check the diff of the recent commits to understand the changes more thoroughly. Let me also examine the remaining relevant files: BrowserView.swift, RawWebView.swift, ConnectionGateView.swift, AuthManager, TailnetProxyPolicy.

--- ANALYSIS CHUNK ---

Now let me check the git diffs of the recent commits to understand what changed, particularly TSNetManager in b4da483 and 90361a8. Let me also check the ConnectionGateView and AuthManager, plus RawWebView.

--- ANALYSIS CHUNK ---

Now I have a good understanding of the recent changes. Let me check a few more important things:

1. The 5071bac commit for web session preservation
2. RawWebView, ConnectionGateView, AuthManager
3. Check `model.activeObservationGeneration` initialization — `TSNetModel.activeObservationGeneration: UInt64 = 0` and `foregroundRecoveryGeneration: UInt64 = 0` — initial generation is 0 for both. Consumer created with default `observationGeneration: 0`.

Interesting: in `willEnterBackground`, `foregroundRecoveryGeneration &+= 1` then `model.activeObservationGeneration = foregroundRecoveryGeneration`. So generation 1.

But wait — there's a subtle issue in `recoverLocalAPIObserversAfterResume`: it sets `model.activeObservationGeneration = generation` where `generation` is the generation captured at willEnterForeground. But `scheduleBusRestart` guards `generation == model.activeObservationGeneration`. OK.

Now key things to trace:

**A. The startup generation check in `tailscaleUp`:** `let generation = model.activeObservationGeneration` — that's 0 at startup. `startEventBus(localAPI:consumer:generation: 0)`. In `startEventBus`, `expectedGeneration = 0`. Then `await MainActor.run { guard expectedGeneration == model.activeObservationGeneration, !didEnterBackground ...}`. At startup, `model.activeObservationGeneration` is 0 (initial). OK.

**B. In `recoverLocalAPIObserversAfterResume`** — note that `consumer = newConsumer` replaces the consumer, but the `busErrorWatcher` from a previous `startEventBus` call is cancelled before. Then `startEventBus(localAPI: client, consumer: newConsumer, generation: generation)`. Inside startEventBus, the sink is on `newConsumer.$error`, and in `await MainActor.run` it cancels `busErrorWatcher` and installs the new one. OK.

Wait — there's a subtle issue with `scheduleBusRestart` being called from the Combine sink. The sink is `await consumer.$error.sink { ... }`. Note: `consumer` is an actor (TSNetConsumer is an actor). `@MainActor @Published var error`. `await consumer.$error` — accessing a @MainActor property from the LocalAPIClient actor context via await, so the sink closure itself... `$error` projected value is a publisher. The `.sink` callback runs on whatever thread publishes; `@Published` publishes on the actor's executor... Hmm, actually `@MainActor @Published var error` publishes on MainActor. The sink closure calls `self.scheduleBusRestart(...)` which is `@MainActor`. But the sink closure is not MainActor-isolated... Let me check: `self.scheduleBusRestart` is `@MainActor private func`. Calling it from a non-isolated context requires `await`. The sink closure `{ [weak self] error in ... self.scheduleBusRestart(...) }` — is this a synchronous closure? Combine's sink closure is `@Sendable (Output) -> Void`? Actually `sink(receiveValue:)` takes `(Output) -> Void`. Calling a `@MainActor` func synchronously from there would be a compile error unless the closure is inferred as `@MainActor`... Hmm, in Swift 6, if the publisher is `@MainActor`, the sink closure might inherit... Actually `await consumer.$error` — `$error` is a `Published<Error?>.Publisher`. The `.sink` doesn't specify a scheduler, so `receiveValue` fires on the publisher's isolation context — with `@MainActor @Published`, the value is sent on the main actor. And Swift 6 will infer the closure as `@MainActor @Sendable`... since the compiler can prove it? Actually the code compiled, so it must be fine — either it's inferred MainActor or `scheduleBusRestart` is nonisolated-callable. It says `@MainActor private func scheduleBusRestart`. So the sink closure must be MainActor-isolated for the call to compile. OK, Swift 6 does infer isolation of Combine sink closures in some cases... Actually no, `.sink` closures are not automatically MainActor. Hmm, but it compiles. Let me not dwell — the code builds, so the compiler accepted it. In Swift 6, `Published.Publisher` has `@MainActor` in its... no. Let me just test compile later maybe. Actually, since `@Published` with `willSet` sends from the actor's context, the sink will be invoked on the main actor at runtime regardless of static isolation. OK.

**C. Now the important deadlock analysis:**

`TailscaleNode` is an actor. `close()` is `async throws` — calls `tailscale_close` synchronously on the actor's executor. `tailscale_close` in Go: `TsnetClose` → `s.s.Close()` → `s.close()` — closes netstack, lb.Shutdown() etc. `lb.Shutdown()` can block for a while (up to seconds). That's synchronous C→Go on the TailscaleNode actor's serial executor — blocking ALL other calls on that node (`loopback()`, `debugResetConnections()`, etc.) during shutdown. If close is triggered while another call is in-flight, they serialize. OK.

The bigger claim in the comment: `tailscaleUp` deliberately avoids `node?.up()` because `up()` blocks the actor indefinitely at NeedsLogin. Verified: `TsnetUp` calls `s.s.Up(context.Background())` which waits for Running. Yes.

But note: `tailscale_start` = `TsnetStart` calls `s.s.Start()` → `doInit` → `lb.Start(...)` → and if state is NeedsLogin → `StartLoginInteractive`. So the node emits BrowseToURL itself. Good — the comment's claim checks out.

**D. Now the critical pieces to check for bugs:**

1. `rebuildNodeAfterFailedResume`: sets `self.node = nil`, then `startInFlight = false`, then `try await node.close()`. Note the ordering: `self.node = nil` BEFORE awaiting close. Then `startTailscaleIfNeeded()` — `startTailscale` → `setupNode()` → creates a NEW TailscaleNode — `tailscale_new()` gets a new `sd` handle — with the same state dir. But the OLD server may still be running until close completes... Actually they do await `node.close()` BEFORE calling `startTailscaleIfNeeded()`. Order in code: `self.node = nil; startInFlight = false; do { try await node.close() } catch...; guard...; startTailscaleIfNeeded()`. OK so close is awaited first. But there's a window where `self.node == nil` while old node is closing. During that window, if another path calls... e.g. `willEnterBackground` (fine, it doesn't touch node), `willEnterForeground` — guarded by generation (which is unchanged... wait, willEnterForeground checks `guard didEnterBackground else return`; if app goes to background during rebuild, didEnterBackground = true, generation bumped, then foreground: `guard let node else { startTailscaleIfNeeded() }` — node is nil! So it calls startTailscaleIfNeeded — creating a NEW node while the OLD one might still be closing? Let's trace: rebuild sets node=nil, awaits close. Suppose during `await node.close()` the app backgrounds: willEnterBackground bumps generation. Then foreground: didEnterBackground = true → false; `guard let node` fails (node == nil) → `startTailscaleIfNeeded()`. startInFlight was set false... so it starts: `startTailscale()` → `setupNode()` → creates new TailscaleNode while old `node.close()` may still be in progress. Two tsnet servers on the same state dir! The old close eventually completes, but there's an overlap window. Also, after `close()` completes, the rebuild path does `guard generation == foregroundRecoveryGeneration` — that FAILS now (generation was bumped), so it returns without calling startTailscaleIfNeeded — good, because the other path already started one. But the overlap: new server starting on the same state dir while old one closes. tsnet state store is a single JSON file; two live servers both writing to it could corrupt or fight. Also both create magicsock etc. The Go `servers.m` map keeps both. Window is small but real. Severity: MEDIUM/HIGH — actually let me think about whether close can take long: `s.close()` → shutdownCtx cancel, netstack.Close, lb.Shutdown — Shutdown waits for goroutines; control client etc. Could take seconds, particularly if network is wedged. Meanwhile the new server does `lb.Start` with the same state store — store.New reads the same file. It might read while old one writes. Risk of state corruption or two nodes briefly online with the same identity (same node key!) — both connected to control with the same node key — that could cause control-plane confusion (the control plane handles same-key reconnects by kicking the old one). It's a real race, but narrow.

2. `willEnterBackground` while `startTailscale()` in flight: `startTailscale` is `nonisolated`, running on a background task. `tailscaleUp` checks `didEnterBackground` after `startEventBus` — reads `@MainActor private var didEnterBackground` from nonisolated context?? Wait: `func tailscaleUp(localAPI:consumer:) async throws` — is it MainActor? The class is `@MainActor final class TSNetManager`. `func tailscaleUp` has no explicit annotation, so it's MainActor-isolated. It's called via `try await tailscaleUp(...)` from `startTailscale` (nonisolated). OK so tailscaleUp runs on MainActor. `startEventBus` also MainActor? `func startEventBus(localAPI:consumer:generation:) async throws` — no annotation → MainActor. But it does `try await localAPI.watchIPNBus(...)` — suspension. OK.

Hmm wait, actually there's a subtlety: `tailscaleUp` is MainActor, and calls `try await self.node?.loopback()` — `loopback()` is NOT async in TailscaleKit (`public func loopback() throws -> LoopbackConfig` — it's a sync actor-isolated method!). Interesting: `TailscaleNode.loopback()` is synchronous but actor-isolated. Calling `try await self.node?.loopback()` crosses the actor boundary — but the call itself is synchronous C→Go on the TailscaleNode actor. Fine.

BUT — WAIT. There's a serious one: `loopback()` is a synchronous actor method. `tailscaleUp` runs on the MainActor. `try await self.node?.loopback()` — fine, hops to node actor, runs synchronously, returns. OK.

And `debugResetConnections()` — synchronous, actor-isolated, `try await node.debugResetConnections()` from MainActor in willEnterForeground's task — blocks the TailscaleNode actor for up to 5s (Go deadline). During that time, ALL LocalAPI calls that need the node (basicAuthURLRequest → tailscaleSession → node.loopback()) QUEUE behind it. loopback() is cached so returns immediately... but still serialized on the actor. So during the 5s reset, `watchIPNBus`, `backendStatus` etc. block waiting for the actor. In `willEnterForeground`, recovery awaits debugResetConnections FIRST, then recoverLocalAPIObservers — so no self-deadlock there since sequential. But `recoverLocalAPIObserversAfterResume` is also called after. OK.

However — `startStatusPolling` and bus restart tasks could call `backendStatus()` concurrently with `debugResetConnections` — they'd queue up to 5s. Not a deadlock, just delay. Fine.

3. **The CRITICAL one to check: `MessageReader.stop()` vs `deinit`.** MessageProcessor.deinit calls `cancel()` + `reader.stop()`. cancel() sets cancelled under lock, calls reader.stop() — adds operation to workQueue. OK.

**Ordering issue in `MessageProcessor.start()`:** the start block runs on workQueue: calls `self.cancel()` FIRST (which calls `reader.stop()` — adding a stop operation to the READER's workQueue) then `reader.start(...)` (adding start op). Since both are addOperation on a serial queue, stop runs before start. Good ordering.

4. **`MessageReader.receiveData` buffer indexing bug**: `if buffer[buffer.count - 1] == kJsonNewline` — Data indexing! `buffer` is `Data`, and `buffer[buffer.count - 1]` uses... Data's subscript with Int — since Data conforms to Collection with index type Int? Actually `Data` conforms to `Collection` where Index == Int, BUT the subscript uses the actual index. If `buffer` was created as `Data()` and only appended to, startIndex is 0, so `buffer[count-1]` is the last byte. OK fine here since buffer is freshly created and emptied via removeAll. Fine.

Also note: `receiveData` only completes a message when buffer ENDS with newline. IPN bus sends newline-delimited JSON. If a message arrives without trailing newline (partial), it stays in buffer — OK.

But — congestion handling: `if pendingMessages.count >= kMaxQueueSize { congested = true; messagesAvailableHandler?(); return }` — note when congested it DISCARDS the just-received complete buffer? Let's see: buffer holds a complete message (ends with newline), queue full → congested = true, and... `return` — the buffer is NOT appended and NOT cleared! So the completed message stays in buffer. Then subsequent `didReceive data` returns early `if congested`. The buffer keeps growing? No — didReceive returns early before receiveData when congested, so no more appends. When drained: `drain` resets congested = false after delivering messages. Then next didReceive appends to buffer, which still has the old complete message at the front. Eventually buffer ends with newline → appended. So the message is delayed, not lost. Hmm OK.

But actually there's an interesting one: when congested is set, `messagesAvailableHandler?()` is called — this triggers drain via MessageProcessor. drain delivers pendingMessages and then calls errorHandler(MessageQueueError.queueCongested) — the errorHandler → `consumer.error` → triggers bus restart. OK.

5. **URLSession delegate queue `workQueue` and `drain` ordering**: delegate callbacks arrive on workQueue (delegateQueue: workQueue). didReceive → receiveData → addOperation on same queue. drain also addOperation. Serial queue — ordering preserved. OK.

BUT `stop()` — "Do not cancelAllOperations". `stop` adds an operation that invalidates the session and nils the task. Any pending didReceive/receiveData operations queued BEFORE stop will run before stop (FIFO) — fine, they mutate buffer/pendingMessages which then get cleared by stop. After stop, session invalidated, no more callbacks. OK.

**However:** `MessageReader.drain` after stop: `MessageProcessor.messagesAvailable` may already be in flight... `drain` completion delivers messages to `processMessage` which posts to the PROCESSOR's workQueue and calls `consumer.notify`. The consumer checks generation. OK — stale events discarded via generation check. Good.

6. **`TSNetConsumer.error` and `@MainActor @Published`** — `consumer.$error` sink: `await consumer.$error` — this captures the publisher once. In `recoverLocalAPIObserversAfterResume`, a NEW consumer is created each generation; old consumer's sinks were tied to the old busErrorWatcher, cancelled. OK.

7. **Now, the `startEventBus` + `scheduleBusRestart` potential duplicate-watcher issue:**

`scheduleBusRestart` runs on MainActor. It cancels `busRestartTask`, clears consumer.error, spawns new task. In the task: `startEventBus(...)` → inside, a NEW sink on `consumer.$error` is created and (via MainActor.run) replaces `busErrorWatcher`. BUT: the sink fires when `consumer.error` is set. `consumer.error = nil` was cleared in scheduleBusRestart. Hmm, but note: TSNetConsumer.error(error:) — the actor method — sets `self.error = error` only if generation matches. Fine.

Wait — there's a leak: each `startEventBus` call creates a new sink (`busObserver`) on the SAME consumer.$error. The old sink is cancelled via `busErrorWatcher?.cancel()`. But in the `scheduleBusRestart` path, `startEventBus` is called from the busRestartTask; inside startEventBus, `busObserver = await consumer.$error.sink {...}` — then `await MainActor.run { guard expectedGeneration == model.activeObservationGeneration ... busErrorWatcher?.cancel(); busErrorWatcher = busObserver }`. OK replaced.

But here's a subtle one: `consumer.$error` — each new sink SUBSCRIBES to the same @Published publisher. If generation mismatch occurs at the MainActor.run guard, busObserver.cancel() and processor.cancel() — fine.

8. **The watch-ipn-bus 60s URLSession timeout cycle**: `doSimpleAPIRequest` has `timeoutInterval: 60` default. But `watchIPNBus` uses `basicAuthURLRequest` and creates its own session without setting request.timeoutInterval — default 60s. So the long-poll dies every 60s → didCompleteWithError → errorHandler → consumer.error → scheduleBusRestart. Expected churn.

Hmm wait — does `didCompleteWithError` with NSURLErrorTimedOut (-1001) fire for the long poll? Yes after 60s without completion. Then restart. That matches the comment "kills it every minute of idleness".

9. **`backendStatus()` polling every 5s** — each call creates a NEW `URLSession(configuration: sessionConfig)` — never invalidated! `URLSession(configuration:)` sessions that are never `invalidateAndCancel`'d or `finishTasksAndInvalidate`'d leak: session retains delegate (nil here) and connection cache. Every 5 seconds a new session. That's a session leak — HIGH-ish. Actually let me double check `doSimpleAPIRequest`: `let session = URLSession(configuration: sessionConfig); let (data, response) = try await session.data(for: request)`. No invalidation. For `async` data(for:), URLSession uses its delegate queue etc. Sessions without invalidation persist for app lifetime — each holds memory (connection pool etc.). Every 5s for status polling + every bus restart + prefs calls... This is upstream TailscaleKit code, but still: a memory/resource leak. Over 8 hours: 5760 sessions. Each maybe a few KB + possibly socket pools. MEDIUM finding. And it's pinned third-party code; the fix should go upstream or the app should create one session and reuse.

Actually wait — for LocalAPI polling the app could hold a single URLSession. Let me flag it.

10. **The socks proxy `pump` recursion**: `pump(from:to:)` — receive → on data: observe, then `to.send(content:completion: .contentProcessed { pump again })`. Backpressure: only re-receives after send completes. Good — this provides backpressure. BUT: recursion via callback — each iteration adds a new closure call on the queue; no stack growth since async. OK.

Half-close: `isComplete` — when `from` delivers isComplete (EOF), both `from.cancel()` and `to.cancel()` — cancelling `to` kills the other direction immediately, dropping any in-flight data from the other pump (e.g., server still sending response body when client half-closes). For HTTP/1.1 over SOCKS, if client sends RST... In practice WebKit closes both directions at once. But a half-close from tsnet side (e.g., server done sending, closes) → cancels client connection — drops unsent data in the client→upstream direction. That's a minor correctness issue: abrupt close instead of graceful half-close propagation (`to.send(content:completion:)` then cancel after final send). Also cancel discards data already sent but not yet processed? `NWConnection.cancel()` drops pending sends. So in-flight data loss on race between the two pumps. MEDIUM/LOW.

Also: when one pump cancels both connections, the other pump's receive completes with error → calls from.cancel/to.cancel again — double cancel is safe for NWConnection. OK.

**Waiting clients gate**: `handle(client:)` — when `upstreamAvailable == false`, client added to waitingClients with stateUpdateHandler that removes on .failed or .cancelled. No timeout! A waiting client can sit forever if tsnet never returns to Running — e.g., user stays logged out. WebKit might hold the connection open for minutes (its own timeouts? WebKit has its own load timeouts — but the TCP connection to the relay is established; the relay never reads, so WebKit's request never times out at the network level... Actually WebKit/CFNetwork has its own resource load timeout (default 60s for URLSession; WKWebView loads have their own timeout ~ no explicit timeout; navigation can hang for a long time). The page load spinner sits. That's by design ("holds WebKit's request without returning a SOCKS failure"). But: **leak risk** — waitingClients unbounded: every new navigation attempt while gate closed adds a connection. If WebKit gives up and closes, .failed/.cancelled removes it. OK.

But there's a subtle bug in the waiting-client stateUpdateHandler: `client.stateUpdateHandler` for waiting clients handles `.failed` and `.cancelled` but NOT `.ready` — fine (nothing to do). But when the gate OPENS, `setUpstreamAvailable(true)` → `beginRelay(client:id:)` — sets `client.stateUpdateHandler = nil` and starts pumps. But what if the client connection FAILED while waiting and the .failed handler was queued but not yet executed on the serial queue, and the gate-open handler runs first? Order: both async on same serial queue — if .failed callback is queued first, it runs first (removes from waitingClients, so gate-open won't find it). If gate-open runs first, beginRelay starts pumps on a dead connection — pumps immediately get errors → cancel both. Harmless.

What about `waitingClients` and `stop()`: stop cancels all waiting clients. OK.

11. **SocksLogProxy.start() called from `proxyConfig()`** — which is called from `tailscaleUp` (MainActor... wait, `proxyConfig` is called inside `await MainActor.run { model.proxyConfiguration = proxyConfig(loopback) }`). `proxyConfig` is nonisolated? It's `func proxyConfig(_ loopbackConfig:) -> ProxyConfiguration?` — no annotation → MainActor (class is @MainActor). It calls `relay.start()` — which BLOCKS on a semaphore up to 3s waiting for the listener. On the MAIN ACTOR. `ready.wait(timeout: .now() + 3)` — blocks the main thread up to 3 seconds during startup! The listener usually becomes ready in ms, so normally fine, but on a pathological system (or if NWListener is slow), the main thread hangs 3s during app launch. Also this is called from `refreshProxyPolicyIfNeeded`? No — that only updates matchDomains. And from `tailscaleUp`. Only once per node. LOW/MEDIUM.

12. **proxyConfig() republishing:** `applyProxy(_ proxy:)` in BrowserViewModel: `dataStore.proxyConfigurations = [proxy]` — "if !didLoadInitial { loadInitial() }". OK.

But `refreshProxyPolicyIfNeeded` republishes `model.proxyConfiguration = updated` whenever the rule set changes — every time a peer joins/leaves (status poll every 5s → refreshProxyPolicyIfNeeded → policy != model.proxyPolicy → republish). Comment says sorted to avoid spurious republishes. When it DOES republish (peer joined), `dataStore.proxyConfigurations = [proxy]` — does setting proxyConfigurations on a live dataStore drop in-flight connections? Per WebKit behavior, changing proxy configs tears down existing network sessions/connection pools? The commit 5071bac said "proxy policy republishes" were a reason pages reload. Let me check what applyProxy does now: just sets dataStore.proxyConfigurations and loads initial if not loaded. Setting `WKWebsiteDataStore.proxyConfigurations` mid-flight: per WebKit docs/source, it reapplies to new connections; existing connections... In WebKit's NetworkSession, updating proxyConfigurations recreates the NetworkSession? Let me check WebKit source behavior — `WebsiteDataStore::setProxyConfigurations` → `networkSession().setProxyConfigurations` → in NetworkSessionCocoa, setting proxy configurations causes `m_sessionWithProxy` recreation... Actually in WebKit, NetworkSessionCocoa has multiple sessions keyed by proxy config; changing proxy config doesn't kill existing connections but new connections use new config. I recall WKWebsiteDataStore.proxyConfigurations being designed for runtime updates (added iOS 17). Let me not overstate; I can note uncertainty.

Actually the "tabs sent home" reports — the key analysis. Let me trace all paths that can reload/reset a tab:

- `BrowserViewModel.loadInitial()` — called from applyConnectionState when connected && !didLoadInitial, and applyProxy when !didLoadInitial. didLoadInitial set true on first load; reset to false in unloadWebView. So after unloadWebView (hidden tab), reselect → makeWebView → pendingLoadURL loaded (the tab's committed URL, not home). OK preserved.

- `unloadWebView()`: `if let url { pendingLoadURL = url }` — url is the validated address-bar URL. If `url` is nil (page never committed anything — e.g., tab created but never loaded because gate), pendingLoadURL stays nil → makeWebView → loadInitial() → loads initialURL — home! But that's a tab that never showed anything. Hmm.

Wait — actually here's a big one: **tab restore from persistence** — `TabManager.init` restores tabs from `WorkspaceStore.loadTabs` — records with url = `displayURL` — the last committed URL. Good. But `refreshDisplayed` only updates displayURL when viewModel.url changes. If a tab was loaded and user navigated, displayURL tracks. OK.

**The "tabs return home" plausible causes:**

(a) `BrowserTab.stored` uses `displayURL` — updated only via `refreshDisplayed()` when viewModel.$title/$url publishes. If the app is killed while a tab's viewModel.url advanced but... it publishes. OK.

(b) `TabManager.init` — `if tabs.isEmpty { openChatTab(select: true) }` — if `WorkspaceStore.loadTabs` returns nil or empty or ALL records fail URL parse (`URL(string: record.url)` nil) → tabs.isEmpty → opens home tab → user perceives "tabs reset to home". When could all records fail? Corrupted plist, or URLs that don't parse. Possible but rare.

(c) `WorkspaceRoot` gating: `hasConnected` State — on workspace switch & back, a NEW WorkspaceRoot view is created (`.id(ws.id)`), so `@State hasConnected = false` initially... `onAppear { if statusViewModel.running { hasConnected = true } }` — if the workspace is Running, hasConnected flips on appear — fine. But if the workspace is NOT running at that moment (reconnecting after resume), `hasConnected` stays false → shows ConnectionGateView instead of the browser! The user had tabs open; after switching workspaces during a reconnect, they see the GATE (login/onboarding screen) — and the gate... does it reset tabs? No. When running flips true, hasConnected → true → browser returns with tabManager intact (tabs preserved since workspace owns tabManager). But wait — `onDisappear { tabManager.unloadAllWebViews() }` — when switching away, all webviews unloaded. On return, `tabManager.currentTab` — BrowserRootContent → BrowserView → RawWebView → makeWebView → loads pendingLoadURL (committed URL). So URL preserved... but the page RELOADS (fresh WKWebView). User perception: page reloaded — not "home", but a reload. If pendingLoadURL was nil → loadInitial → initialURL → **home page**. When is pendingLoadURL nil but didLoadInitial was true? unloadWebView sets pendingLoadURL = url (may be nil if nothing committed). If tab had committed URL, url non-nil. OK.

Hmm wait, actually there IS a subtle one in unloadWebView: `if let url { pendingLoadURL = url }` — `url` is `@Published private(set) var url: URL?` — the address-bar URL. Suppose the tab navigated A → B. url = B. unload → pendingLoadURL = B. makeWebView → loadResolved(B). Good. But suppose navigation B → C FAILED (provisional fail): url stays B; the error overlay shows C. Unload → pendingLoadURL = B. Reselect → loads B. Reasonable.

(d) **`refreshDisplayed` overwriting displayURL with initialURL?** No.

(e) **Reload via `applyConnectionState`:** connected=true && !didLoadInitial → loadInitial → only when not loaded. didLoadInitial stays true across bounces. OK.

(f) **webViewWebContentProcessDidTerminate** → navigationError → overlay with Retry; doesn't auto reload. OK.

(g) **Workspace switch: `.id(ws.id)`** — tears down subtree; new WorkspaceRoot → new StateObject tabManager? NO — `_tabManager = StateObject(wrappedValue: workspace.tabManager)` — workspace.tabManager is lazy, persists. Good.

(h) **The BIG one — `Workspace` deinit / multiple workspaces**: All workspaces live concurrently, each with its own TSNetManager and tsnet node. When app backgrounds, `WorkspaceManager.willEnterBackground` fans out to ALL workspaces — each does the full teardown. On foreground, ALL workspaces run `debugResetConnections` + observer recovery simultaneously. That's N nodes revalidating at once. Fine-ish.

(i) **Now — the strongest "sent home" candidate: `TabManager` restoration + `openChatTab` when `tabs` all fail to restore, AND `BrowserViewModel.makeWebView` when `pendingLoadURL == nil && didLoadInitial == false` → `loadInitial()`** which loads `initialURL`. And note `loadInitial` requires `isConnected`. If not connected, it waits for the state flip... then loads initialURL. So a restored tab whose webview is created while disconnected loads its initialURL once connected — the RESTORED URL (initialURL = record.url). So still the persisted URL. OK good.

(j) What about **`makeWebView` calling `loadInitial()` when pendingLoadURL is nil but the tab HAD a URL** — covered above: pendingLoadURL set from url. Hmm, one hole: `unloadWebView` is called for hidden tabs at `select()`. At that point url is committed URL — good.

But WAIT: **hidden tabs that were never selected since restore have no webview** — `hasWebView` false. `unloadHiddenTabs` → `tab.unloadWebView()` → `viewModel.unloadWebView()` → `guard let webView else { return }` — no-op. Good.

(k) **`BrowserRootContent` keyed `.id(tab.id)` on the toolbar only; `BrowserView(model:)` with `.id(ObjectIdentifier(model))`** — switching tabs creates new RawWebView representable, calling makeWebView for that tab. OK.

(l) **WebKit process termination while hidden**: unloadAllWebViews onDisappear... 

(m) **A REAL suspect for "tabs sent home": the `ReconnectingBanner`/gate + `loadInitial` interplay** — `applyConnectionState(false)` doesn't touch webview. `applyConnectionState(true)` → `if !didLoadInitial { loadInitial() }`. didLoadInitial is false if... `unloadWebView` set it false. So: hidden tab unloaded (didLoadInitial=false, pendingLoadURL=URL). Reselected → makeWebView → pendingLoadURL non-nil → loadResolved(pendingLoadURL) — hmm but this happens even when NOT connected! `makeWebView` doesn't check isConnected! So a tab reselected during reconnect loads its URL immediately → the SOCKS gate holds it → when gate opens, load completes. Fine.

But `loadInitial` gating `needsPeerDataToRoute`: for bare hostnames (no dot) — holds until peer data arrives. But `loadResolved(pendingLoadURL)` BYPASSES that hold! A restored tab on `http://ai/` reselected before peer data → loads immediately → bare name `ai` not in matchDomains yet (policy is ipRangesOnly) → goes DIRECT → DNS fails or worse, resolves to PUBLIC ai?? Wait — bare label `ai` via public DNS won't resolve (NXDOMAIN) → nav error. BUT if the bare label is a public TLD-withheld name (peer named `ai`), `expandWithheldShortName` handles it in `resolveForTailnet` — only called from `load(url:)`, NOT from `loadResolved`! Hmm: `makeWebView` → `loadResolved(pendingLoadURL)` — pendingLoadURL was already resolved at load time (resolveForTailnet applied before storing? No! `load(url:)` → `let target = resolveForTailnet(url)` → pendingLoadURL = target — resolved target stored. And unloadWebView stores `url` — the COMMITTED URL (already expanded FQDN if expansion happened). So pendingLoadURL is already-expanded. OK.)

So the direct-DNS race: restored tab with bare hostname `http://nas/` (not a public TLD, so it IS in matchDomains once peer data arrives): reselected immediately after launch/reconnect before first status poll → policy = ipRangesOnly → `nas` doesn't match → DIRECT → WebKit resolves `nas` via public DNS → NXDOMAIN → **navError**... and if !isConnected, navigationError() returns early (no overlay) — but the load still failed; page shows... nothing (blank/new tab page), no overlay, no auto-retry! didLoadInitial... wait didLoadInitial was false; loadResolved doesn't set didLoadInitial. Hmm — `loadInitial` sets didLoadInitial = true. `makeWebView` with pendingLoadURL → loadResolved directly, didLoadInitial stays false. Then when connected flips true → applyConnectionState(true) → `if !didLoadInitial { loadInitial() }` → loadInitial loads initialURL — **THE INITIAL URL, not the pending URL!** Wait: initialURL is the tab's initialURL — for a restored tab it's the persisted record.url (same as pendingLoadURL roughly). But for a tab where user navigated A → B: pendingLoadURL = B, initialURL = A. Scenario: app killed & relaunched → tab restored with record.url = B → initialURL = B. OK same. But WITHIN a session: hidden tab unloaded (pendingLoadURL = B, initialURL = A). App backgrounded, foregrounded → reconnecting. User selects tab → makeWebView → loads B immediately (gate holds). Gate opens → B loads. didLoadInitial false. Then connection bounce again → applyConnectionState(true) → !didLoadInitial → loadInitial() → loads A — **the tab's ORIGINAL home URL!** THE TAB GOES HOME!

Wait let me double-check `loadInitial`: `guard !didLoadInitial, isConnected else { return }`; `didLoadInitial = true; load(url: initialURL)`. And makeWebView: `if let pendingLoadURL { self.pendingLoadURL = nil; loadResolved(pendingLoadURL) } else { loadInitial() }` — does NOT set didLoadInitial when loading pendingLoadURL.

So after ANY unload+remake with pendingLoadURL, didLoadInitial remains false forever, and the next connection-state flip to Running (bounce, resume-from-background, proxy policy republish... wait applyConnectionState only fires on `.map { $0 == .Running }.removeDuplicates()` — so a false→true transition) triggers loadInitial → loads initialURL → **user's page replaced by the tab's original URL ("sent home")**.

Hmm wait, but hold on: does `unloadWebView` get called for the SELECTED tab in normal operation? `unloadHiddenTabs` only unloads non-selected. `unloadAllWebViews` on workspace switch. Background? `willEnterBackground` doesn't unload webviews. So the sequence requires: select tab T (loads A, navigate to B), switch to another tab or workspace (T unloaded), switch back (T recreated, loads B via pendingLoadURL, didLoadInitial=false), then ANY state flip Running→not→Running (e.g. lock phone, unlock — willEnterBackground sets model.state = .Starting; willEnterForeground sets .Running) → loadInitial fires → loads A. **TAB SENT HOME.** That's a CRITICAL finding — matches the prompt's "plausible reports that tabs return home".

Let me double check the state flow: willEnterBackground → `model.state = .Starting` → BrowserViewModel.applyConnectionState(false) — wasConnected true → logs "reconnecting". willEnterForeground → recovery → `self.model.state = .Running` → applyConnectionState(true) → `if !didLoadInitial { loadInitial() }`. For the reselected tab with didLoadInitial=false → loadInitial → guard isConnected true (isConnected just set true) → needsPeerDataToRoute check — initialURL likely has a dot or passes → didLoadInitial = true, load(url: initialURL) — REPLACES page B with A. YES. CRITICAL bug. The UI tests pass because they never unload the webview before the bounce (single tab, never hidden). testWorkspaceTabsSurviveSwitching doesn't background. testBackgroundResumeReconnectsWithoutReloadingPage uses a single tab that was never unloaded. 

Also `applyProxy` — `if !didLoadInitial { loadInitial() }` — fires when proxyConfiguration republished (policy change — peer join!). So even WITHOUT backgrounding: select T (A→B), switch tabs, switch back (loads B), a peer joins → status poll → refreshProxyPolicyIfNeeded → policy changed → model.proxyConfiguration republished → applyProxy → loadInitial → A. TAB SENT HOME without any lifecycle event! Even more likely. And `proxyConfiguration` is also republished... wait — `applyProxy` is subscribed to `model.$proxyConfiguration.compactMap`. refreshProxyPolicyIfNeeded sets `model.proxyConfiguration = updated`. Yes.

Wait, but actually check

--- ANALYSIS CHUNK ---

The user wants me to write the complete review now based on what I've inspected. Let me organize my analysis of what I've found across all the files I've read.

Let me trace through the key findings carefully before writing:

**1. Deadlocks/hangs/crashes:**

- `SocksLogProxy.start()` uses `ready.wait(timeout: .now() + 3)` — a semaphore wait on the CALLING thread. Who calls `start()`? `proxyConfig(_:)` in TSNetManager, which is called from `tailscaleUp` → `if let loopback = try await self.node?.loopback() { await MainActor.run { model.proxyConfiguration = proxyConfig(loopback) } }` — so `proxyConfig` runs ON THE MAIN ACTOR. Inside it, `relay.start()` blocks the main thread for up to 3 seconds waiting for the NWListener to become ready. The listener's stateUpdateHandler fires on `queue` (the relay's serial dispatch queue), so no deadlock (the handler isn't on main), but a 3-second main-thread block during startup. Worse: `l.state` is read from the main thread while the listener state is updated on `queue` — reading `l.state` synchronously... Actually NWListener.state — reading it from another thread. There's also a subtle issue: after `ready.wait` returns due to `.ready` signal, `l.state` should be `.ready`. If timeout fires, state could still be `.setup` → they cancel and return nil → falls back to tsnet direct. OK. But the 3s main-thread block is real: `proxyConfig` is called on MainActor via `await MainActor.run { model.proxyConfiguration = proxyConfig(loopback) }`. Also called in `refreshProxyPolicyIfNeeded`? No, that one doesn't call relay.start (only if socksLogProxy == nil). Actually `proxyConfig` is only called once at startup (tailscaleUp). Hmm, but it's called from `await MainActor.run` in tailscaleUp — main thread block up to 3s. Impact: startup stall. MEDIUM.

- `fatalError` in `startTailscale()` catch block — any setup error crashes the app. In `rebuildNodeAfterFailedResume`, `startTailscaleIfNeeded()` → if the rebuild fails (e.g., tailscale_start fails transiently), the app crashes with fatalError. That's a crash path in a "recovery" path. HIGH. Also note `startInFlight` reset: in the catch, `await MainActor.run { startInFlight = false }` then `fatalError` — so it crashes anyway. On rebuild path: `rebuildNodeAfterFailedResume` sets `startInFlight = false` then calls `startTailscaleIfNeeded()` which sets startInFlight = true and spawns task → `startTailscale()` → `setupNode()` creates a NEW TailscaleNode via `try TailscaleNode(config:...)`. If that throws → fatalError → crash. If the state dir is fine this should succeed... but could fail if the old server hasn't fully released state or ports. Also worth noting: the old node close happens BEFORE startTailscaleIfNeeded, good. But `node.close()` → `tailscale_close` → `TsnetClose` → `s.s.Close()` → can block up to 5s (logtail shutdown ctx) — called via `try await node.close()` on the TailscaleNode actor — synchronous C call, blocks the actor; other calls queue behind. Not main actor though. OK.

- `TailscaleNode.deinit` calls `tailscale_close(tailscale)` — and `close()` also calls it. `TsnetClose` deletes from the map then calls `s.s.Close()`; `tsnet.Server.Close()` uses `closeOnce` so double close returns error the second time (`net.ErrClosed`), but `TsnetClose` on the same sd twice: second call gets `s == nil` → returns `C.EBADF`. But deinit happens after close() was already called in rebuild path... Deinit calling tailscale_close on an already-closed sd: `TsnetClose` returns EBADF — harmless. But actually a REAL issue: after `close()` succeeds, the Swift `TailscaleNode` object is set to nil in `rebuildNodeAfterFailedResume` (`self.node = nil` BEFORE `try await node.close()` — wait let me re-check the order:

```swift
self.node = nil
startInFlight = false
do {
    try await node.close()
} catch { ... }
guard generation == ... 
startTailscaleIfNeeded()
```

`self.node = nil` is set first, then `node.close()`. Since `node` parameter holds a strong ref, deinit won't happen until after close. OK. But `localAPIClient = nil` releases the client which holds `node` ref too. Fine.

BUT: what if the generation check fails after close (user backgrounded during close)? Then `self.node` is nil, `startInFlight = false`, and no new node is started. On next `willEnterForeground`, `guard let node else { startTailscaleIfNeeded(); return }` — node is nil → startTailscaleIfNeeded → creates fresh node. OK that works.

What if the app is backgrounded while `foregroundRecoveryTask` is awaiting `node.debugResetConnections()`? willEnterBackground cancels the task, but "Swift task cancellation cannot interrupt a synchronous C/Go call" — the call runs to completion (up to 5s deadline in Go). Then `guard !Task.isCancelled, generation == ...` — cancelled → return. But wait: the `self.model.state = .Running` line happens right after the try succeeds, BEFORE the cancellation guard! Look:

```swift
do {
    try await node.debugResetConnections()
    logger.log("Foreground: tsnet transport rebind complete")
    self.model.state = .Running
} catch { ... }
guard !Task.isCancelled, ...
```

If backgrounded during debugResetConnections: willEnterBackground sets model.state = .Starting, then the C call returns, and `self.model.state = .Running` is set AFTER — overwriting the background's `.Starting`. Now the app is in background with state=.Running and the SOCKS gate... willEnterBackground already called `socksLogProxy?.setUpstreamAvailable(false)`. Hmm, but the proxyAvailabilityWatcher observes model.$state — setting .Running while in background triggers the sink → `setUpstreamAvailable(true)` → opens the gate while backgrounded. Then next foreground: `guard didEnterBackground else { return }` — didEnterBackground is true → recovery runs again with a NEW generation, sets state .Running again after rebind. The state overwrite is benign-ish but the gate reopening while suspended is a real (if minor) state inconsistency: new SOCKS clients arriving while suspended would be relayed to tsnet whose transports are frozen. This is a race window of ~milliseconds-to-5s. MEDIUM-ish. Actually the sequence: task cancelled but the code between `try await node.debugResetConnections()` return and the cancellation check executes unconditionally — `self.model.state = .Running` runs even when the generation is stale. Also `proxyAvailabilityWatcher` fires → `if available { self.runConnectionResetTestIfNeeded() }` — the test hook. Minor.

More important: `model.state = .Running` is set WITHOUT any actual verification that the backend is Running — it's "synchronous repair call reached LocalBackend and both rebind actions completed, so the retained proxy is usable." But debugResetConnections only proves the LocalAPI HTTP call to the backend completed — actually wait, does it? `lc.DebugAction(ctx, "rebind")` goes through `s.s.LocalClient()` → in-process client → the localapi handler → `h.b.DebugRebind()` → `b.MagicConn().Rebind()`. Rebind is asynchronous in magicsock (it closes and reopens sockets... actually `Rebind` in magicsock does the rebind synchronously? Let me recall: magicsock `Rebind()` closes existing sockets and rebinds... In tailscaled's implementation, Rebind is essentially synchronous socket re-creation but STUN re-resolution is async. The point: debugResetConnections succeeding does NOT mean DERP is reconnected or WireGuard paths work — it means the *request* succeeded. So the comment "so the retained proxy is usable" is optimistic. The netstack TCP sessions: breaking DERP conns interrupts the underlying transport; existing proxied TCP streams stall until DERP reconnects; if the peer path doesn't come back, the sessions hang and eventually time out — WebKit fetch hangs, no error. But at least no reload. Whether sessions "survive" depends on how fast DERP re-establishes and whether TCP retransmits outlast the gap. This is the review point #8: "whether live netstack TCP sessions actually survive/recover" — answer: rebind+break-derp drops all DERP TCP connections and rebinds UDP; in-flight TCP-over-netstack flows lose whatever packets were in flight; TCP will retransmit once the tunnel returns. If DERP reconnect takes >~30s, WebKit may abort loads. gVisor netstack TCP keepalive/retransmit defaults will keep the session alive for a while, so a few-second blip survives. But the app's *test* (SIGSTOP 7s) proves it survives 7s. Reasonable.

Also on the "rebind" model: iOS suspend doesn't actually kill UDP sockets in a way that rebind fixes? The mechanism is speculative ("iOS may silently kill UDP sockets... without producing a useful netmon change"). The unconditional reset on EVERY foreground is a heavy hammer: every single lock/unlock cycle rebinds magicsock and breaks ALL DERP connections — including healthy ones, interrupting any in-progress tailnet traffic (e.g., an active large download through the proxy) for a second or two. Also `break-derp-conns` when the home DERP connection is the only path (no direct UDP) means all peer traffic stalls until reconnect. Acceptable tradeoff, but worth noting: doing this unconditionally on every resume (even when the app was locked for 1 second with transports perfectly alive) introduces churn. A conditional approach (netmon stale / last-suspend-duration threshold) would be gentler. Also note: foreground recovery calls `debugResetConnections` even if the app was backgrounded for milliseconds (bg/fg bounce from a notification swipe). MEDIUM design note.

**Race: `foregroundRecoveryTask` cancel without setting nil in willEnterForeground... it does cancel() then reassigns. OK.

**Race in willEnterForeground when node == nil**: `startTailscaleIfNeeded()` is called, but note that `didEnterBackground = false` was already set. If a start is in-flight from launch (startInFlight true), it's skipped. But here's a scenario: startup `tailscaleUp` is suspended mid-flight (app backgrounded during first launch, before `startEventBus` returns). willEnterBackground: didEnterBackground=true, generation bump, `processor?.cancel()` (nil), stopStatusPolling (nil), `model.state = .Starting`. Then willEnterForeground: didEnterBackground=false, `guard let node else { startTailscaleIfNeeded(); return }`. If `setupNode` succeeded, node != nil → the foreground recovery path runs `debugResetConnections` then `recoverLocalAPIObserversAfterResume`. Meanwhile the ORIGINAL startup task `tailscaleUp` resumes (it was awaiting `localAPI.watchIPNBus` which... hmm, was the URLSession created before suspend? The MessageReader.start posts to its workQueue). When it returns, the guard `if !didEnterBackground, generation == model.activeObservationGeneration` — didEnterBackground is now FALSE again, but generation: startup captured `model.activeObservationGeneration` BEFORE background (say 0). willEnterBackground bumped `foregroundRecoveryGeneration` to 1 and set `model.activeObservationGeneration = 1`. So the stale startup processor gets cancelled and logged "Startup observers completed after background; foreground recovery owns restart". Good — that path is handled. But: `tailscaleUp` then continues to `if let loopback = try await self.node?.loopback()` → publishes proxyConfig. `proxyConfig(loopback)` reads `model.state == .Running` for setUpstreamAvailable. Fine.

But wait — there's a subtle issue in `recoverLocalAPIObserversAfterResume`: it runs `localAPIClient == nil` check — localAPIClient may be nil because `startTailscale` hasn't reached `setLocalAPIClient` yet (startup raced). It creates a SECOND LocalAPIClient for the same node. Then later startup's `setLocalAPIClient(localAPIClient)` overwrites it. Both clients work (they're just URLSession wrappers), so harmless duplication. But `startStatusPolling` guard `statusPollTask == nil` prevents duplicates. OK.

**`consumer.$error` sink — `@MainActor @Published var error` in TSNetConsumer (an actor)**. In startEventBus: `let busObserver = await consumer.$error.sink {...}` — subscribing to a MainActor-published property from nonisolated context with await. The sink callback `self.scheduleBusRestart(...)` — scheduleBusRestart is @MainActor. The sink closure is not actor-isolated... Actually `consumer.$error` is `@MainActor @Published` — so accessing `.sink` requires MainActor; `await` hops. The sink closure itself: where does it run? Published sinks deliver on the actor where the @Published is isolated? With `@MainActor @Published`, the value changes on MainActor (via `Task { @MainActor in ... self.error = error }` in `error(_:)`), and sink callbacks are invoked synchronously from the willSet — on MainActor. So scheduleBusRestart (@MainActor) is called synchronously on main. OK.

But NOTE: `consumer.error = nil` in `scheduleBusRestart` — clearing the error. And TSNetConsumer.error is @Published @MainActor. scheduleBusRestart is @MainActor. OK.

**Duplicate watchers**: `startEventBus` cancels prior `busErrorWatcher` on MainActor before installing new. But there's a path where TWO sinks exist: `scheduleBusRestart` → `startEventBus` → creates `busObserver` sink on consumer.$error → `await MainActor.run { busErrorWatcher?.cancel(); busErrorWatcher = busObserver }`. Fine. But the OLD consumer may still have a sink: in `recoverLocalAPIObserversAfterResume`, a NEW consumer is created each attempt, and `busErrorWatcher?.cancel()` is called at loop top — but the sink was on the OLD consumer's $error; cancelling busErrorWatcher cancels the latest sink. But `startEventBus` for attempt N installs its sink into busErrorWatcher (via the MainActor.run block) — then attempt fails (no fresh response), loop iterates, cancels busErrorWatcher. OK. But: the sinks captured `localAPI` and `consumer` strongly → each startEventBus call's sink retains consumer+localAPI until cancelled. Cancelled ones release. OK.

Now — `TSNetConsumer.notify` spawns `Task { @MainActor in ... }` per notify. During the window between `processor.cancel()` and... old consumer's queued tasks check `model.activeObservationGeneration == self.observationGeneration` — stale generation → discard + log. Good design.

**The status-poll "fresh response" evidence problem**: `startStatusPolling(generation:)` — the poll loop captures `expectedGeneration` and checks `expectedGeneration == self.model.activeObservationGeneration` when PUBLISHING. But `model.freshLocalAPIResponseGeneration &+= 1` is INSIDE that guard — so a stale poll does NOT count as fresh evidence. Good. BUT: the guard checks `!self.didEnterBackground` too. Hmm — during foreground recovery, `recoverLocalAPIObserversAfterResume` calls `startStatusPolling(generation:)` and then waits up to 4s for `freshLocalAPIResponseGeneration` to change. The status poll sleeps 5s BETWEEN polls — wait, look at the loop: poll → publish → sleep 5s. First poll happens immediately. If the backendStatus request hangs (suspended URLSession behavior — actually it's a NEW URLSession created fresh... the request goes through the proxy to the loopback listener... loopback is local so should respond quickly). The bus watcher with .initialState should also deliver quickly. 4s window is probably OK on a healthy node.

But here's a subtle one: `freshLocalAPIResponseGeneration` is also bumped by `TSNetConsumer.notify` for ANY current-generation IPN event — but ALSO bumped in the SAME notify for stale? No — the stale guard returns first. OK.

However — FALSE FRESH EVIDENCE: `recoverLocalAPIObserversAfterResume` sets `responseMarker = model.freshLocalAPIResponseGeneration` BEFORE `startEventBus`. Any current-generation notify (e.g., the initial-state dump) bumps it → "fresh LocalAPI response received". But ALSO: the status poll started in the SAME attempt bumps it on first successful poll. Both are "current generation", so legit. What about a bump from the previous attempt's late callback? Attempt N-1's consumer had generation = same `generation` (recoverLocalAPIObserversAfterResume uses the SAME generation across attempts — `model.activeObservationGeneration = generation` each attempt, and newConsumer gets `observationGeneration: generation`). So attempt N-1's cancelled processor's queued notifies are STILL current-generation! They pass the stale check and bump freshLocalAPIResponseGeneration, potentially providing false "fresh response" evidence for attempt N even if attempt N's own observer is wedged. Wait — but attempt N-1's observers were cancelled at the top of attempt N's loop (`processor?.cancel()`), and their queued MainActor tasks from BEFORE the cancel could still fire after. Those events are from the OLD URLSession though — if data flowed on it, it wasn't fully wedged... Actually the false-evidence scenario: attempt N-1's bus watcher was actually fine and delivered the initial-state dump, but the MainActor task was queued and hadn't run within the 4s window (main thread busy? unlikely for 4s). More realistically: the notify tasks run quickly. Hmm, but here's a cleaner false-positive: each attempt reuses the SAME generation, so a notify from attempt N-1's watcher that arrives during attempt N's wait window bumps the marker and attempt N declares success without proving attempt N's watcher works. If attempt N-1's watcher was actually alive-but-slow and attempt N's watcher is DOA, we now have a live old watcher... no wait, attempt N-1's watcher was CANCELLED at the start of attempt N. If it was cancelled, no more events flow from it except already-queued ones. So the false evidence can only come from events already in flight. Marginal. But the deeper issue: reusing one generation across attempts weakens the stale-event protection the generation system was built for. LOW-MEDIUM.

**Status poll task leak**: `startStatusPolling(generation:)` guards `statusPollTask == nil` — in `recoverLocalAPIObserversAfterResume`, each attempt calls `stopStatusPolling()` then `startStatusPolling(generation: generation)` — OK. But `stopStatusPolling` cancels the task; a cancelled task may still be in `await client.backendStatus()` — the URLSession data request... cancellation of a Swift Task does NOT cancel an in-flight `URLSession.data(for:)`... actually `session.data(for:)` IS cancellation-aware (it registers a cancellation handler that cancels the URLSessionTask). So fine.

BUT: each `backendStatus()` call creates a NEW `URLSession` (`let session = URLSession(configuration: sessionConfig)`) and NEVER invalidates it! `doSimpleAPIRequest` → `let session = URLSession(configuration: sessionConfig); let (data, response) = try await session.data(for: request)` — no `session.invalidateAndCancel()` or finishTasksAndInvalidate. With no delegate, URLSession releases itself after tasks complete? No — URLSession without delegate... Actually URLSession instances must be invalidated or they leak (they retain their configuration and stay alive until invalidate or process end; in practice, for delegate-less sessions, once all tasks complete, the session may be released when the app releases it — hmm. Apple's docs: "The session object keeps a strong reference to the delegate until your app exits or explicitly invalidates the session. If you do not invalidate the session, your app leaks memory until it exits." With nil delegate, no retain cycle via delegate, but the session itself is a local that goes out of scope after the await — ARC releases it, and deinit of URLSession invalidates. I believe URLSession dealloc cancels its tasks. So probably OK-ish, though creating a fresh URLSession per request every 5s prevents connection reuse to the loopback localAPI (keep-alive lost, TLS n/a). Minor perf. For watchIPNBus, MessageReader holds its session and invalidates on stop. OK.

**MessageReader.stop() race already fixed** (comment says). But look: `MessageProcessor.cancel()` calls `reader.stop()` which enqueues on reader.workQueue. And `start()` also enqueues on processor.workQueue which then calls reader.start on reader.workQueue. Ordering: if cancel() is called while start() operation hasn't run yet on processor.workQueue... start's operation runs later and calls `cancel()` first then resets flags and calls reader.start — enqueue order on reader queue: stop (from cancel) then start (from the operation) → start wins, reader becomes live, but the processor was cancelled → `messagesAvailable` checks `cancelled` flag → guard !cancelled → no drain. But the URLSession task is RUNNING and nobody will ever cancel it now — the reader leaks a live long-poll connection forever! Scenario: `let processor = try await startEventBus(...)` — watchIPNBus calls `messageProcessor.start(request, config:)` which enqueues async on workQueue and RETURNS IMMEDIATELY. Then in `recoverLocalAPIObserversAfterResume`, the generation guard fails → `newProcessor.cancel()` — this sets cancelled=true and calls reader.stop() (enqueued on reader.workQueue). Then the processor's start operation runs on processor.workQueue: calls `cancel()` (resets flags: cancelled=false!) then `reader.start(...)` enqueued AFTER the stop on reader queue → session created, task resumed. `messagesAvailable` → handler → processor.messagesAvailable → stateLock check: cancelled was reset to FALSE by the start operation! So it will drain and process messages to a consumer that... is the current consumer? In the recovery path, the consumer is replaced; the stale processor's consumer is the old one with old generation — discarded at model. BUT the bus watcher is ALIVE and consuming resources, and its error handler will call `processError` → old consumer.error → generation check in `error(_:)` discards. So no state corruption, but a leaked live watch-ipn-bus long-poll connection per occurrence, each holding a TCP conn to the loopback listener and a goroutine in Go, plus its 60s-timeout error→nothing. Over many bg/fg cycles this accumulates Go-side bus watchers (watch-ipn-bus handler goroutines leak server-side until the connection dies). Also `deinit` of MessageProcessor calls cancel() + reader.stop() — but if the processor is leaked (retained by sink closures? busObserver sink retains consumer and localAPI, not processor; processor retained by `self.processor` in manager until replaced)... The key bug: **`MessageProcessor.start()` enqueues the actual session creation asynchronously, so `cancel()` immediately after `start()` races and loses — the session starts anyway.** In `recoverLocalAPIObserversAfterResume` the cancel-after-start happens whenever the generation guard trips (rapid bg/fg churn). Also in `tailscaleUp`: `processor.cancel()` right after startEventBus returns — same race! The bus watcher WILL start despite the cancel. Then nobody holds the processor? In tailscaleUp the cancelled processor is dropped → ARC deinit... `deinit { cancel(); reader.stop() }` — enqueues stop on reader.workQueue AFTER the start operation → session cancelled. OK so deinit eventually cleans up IF the processor is released. In tailscaleUp path: `processor.cancel(); logger.log(...)` — processor is a local, released at scope end → deinit → stop enqueued → cleaned. In recovery path: `newProcessor.cancel()` then `return` — local released → deinit → OK. So eventually cleaned, EXCEPT the window where start operation runs AFTER deinit's stop? start op was enqueued on processor.workQueue FIRST (from watchIPNBus), stop enqueued on reader.workQueue SECOND (from cancel/deinit). processor.workQueue operation calls reader.start which enqueues on reader.workQueue — so reader queue order: [stop(from cancel#1)], possibly [stop(from deinit)], then [start]. START RUNS LAST → session created and NEVER stopped. The processor is deallocated (nobody retains it) but its workQueue operation captured `self` strongly ([weak self] — no, `workQueue.addOperation { [weak self] in guard let self = self else { return } ... }` — weak. If processor deallocated before the operation runs, `self` is nil → operation does nothing → NO start. Race: if deinit happens before workQueue runs the start op → no leak. If the start op runs first (enqueued from watchIPNBus before cancel), it calls `cancel()` then `reader.start(...)` → session live; then deinit's `reader.stop()` enqueued... wait deinit already happened? If the start op is running, it retains self during execution (guard let self). After start op completes, self released → deinit → reader.stop() enqueued → reader queue: start was enqueued BEFORE stop (start's reader.start call happens during the start op, before deinit) → order [start, stop] → session created then invalidated → CLEAN. The BAD case: cancel() called (enqueues reader.stop#1), THEN start op runs (enqueues reader.start), then deinit (enqueues reader.stop#2) → order [stop1, start, stop2] → stop2 cleans it. Actually any deinit after guarantees a trailing stop. So leak only if deinit never happens (retain cycle) — processor retained by manager.processor until replaced. Replaced → released → deinit → stop. Seems eventually consistent. Hmm OK, downgrade: transient double-watchers possible but self-cleaning via deinit. But WAIT — deinit calls `reader.stop()` which enqueues; if reader's workQueue is somehow wedged... no.

Actually there IS a real leak case: `MessageProcessor.deinit` — when is it called? The processor's `workQueue` retains the running operation which retains... operations are [weak self]. consumer retains? No. The reader's URLSession delegate is the reader (retained by session), session retained by reader. processor retains reader. If processor deinits → reader released → but reader is retained by the live URLSession as its delegate! URLSession retains its delegate until invalidated. reader.stop() enqueued during processor deinit invalidates the session → eventually releases. If the stop operation is behind a start operation, fine, it runs. So OK. I'll mention the start/cancel race as a transient (double bus watchers for a window) rather than a permanent leak, with the Go-side watcher accumulation caveat. Actually let me reconsider severity: each leaked-concurrent watcher means the Go backend serves multiple watch-ipn-bus long polls; tsnet's ipn.NotifyWatchOpt with initialState on each. Harmless-ish. LOW.

**The `busErrorWatcher` sink on `consumer.$error` — and recovery creating NEW consumers**: In `recoverLocalAPIObserversAfterResume`, old consumer replaced; old consumer's @Published error may still be set... irrelevant.

**scheduleBusRestart consumer.error = nil race**: `consumer.error = nil` — @MainActor set. Fine.

**busRestartTask vs recovery overlap**: `willEnterForeground` cancels busRestartTask. Good. But `startEventBus`'s MainActor.run block installs a NEW busErrorWatcher even when called from scheduleBusRestart — and scheduleBusRestart's sink was triggered by old consumer... covered by generation guards.

**`foregroundRecoveryGeneration` vs `model.activeObservationGeneration` divergence**: `willEnterBackground` bumps foregroundRecoveryGeneration AND sets model.activeObservationGeneration = same. But `startTailscale` (initial startup) captures generation 0. Nothing else bumps activeObservationGeneration. In `rebuildNodeAfterFailedResume` → `startTailscaleIfNeeded` → `startTailscale` → `tailscaleUp` captures `model.activeObservationGeneration` (current, e.g. 3) → observers installed with generation 3 → consumer (the manager's current `consumer`, replaced in recovery with observationGeneration=3... wait, `tailscaleUp(localAPI: localAPI, consumer: consumer)` uses `self.consumer` — the one from recovery attempt with observationGeneration matching. OK. Consistent-ish.

**SocksLogProxy pump recursion**: `pump(from:to:id:observe:)` — receive → send with `.contentProcessed` completion → recursive `self?.pump(...)` — this is NOT call-stack recursion (it's callback-driven), so no stack overflow. Fine. BUT: **no backpressure handling on the reverse direction** — each direction pumps independently: receive(64KB) → send → receive next. That's actually backpressured (one buffer in flight per direction). OK.

**Half-close bug**: In pump: `if isComplete || error != nil { from.cancel(); to.cancel(); return }`. When one direction sees EOF (isComplete), it cancels BOTH connections — killing the other direction mid-stream. TCP half-close: WebKit sends FIN after its request body but still expects the response. Cancel both → response bytes in flight lost → truncated/failed loads. How likely? Browsers rarely half-close requests, but CONNECT tunnels carry arbitrary protocols; and the SOCKS handshake itself is small. Also error on one side (e.g., upstream reset) cancelling the client side is right. But `isComplete` on client→upstream pump cancels upstream→client — killing data that NWConnection has already received but not yet delivered... `to.cancel()` discards queued outbound data. So a client that sends its request and FINs (some HTTP/1.0-style clients, curl) loses the response. MEDIUM. Fix: on isComplete, `to.send(content: nil, isComplete: true...)` to propagate FIN (NWConnection supports sending contentComplete), and cancel only after both directions complete.

Also: when `data` is empty and NOT complete and no error → recursive re-receive — fine.

**Waiting clients**: gate closed (upstreamAvailable=false) → clients parked in waitingClients with NO timeout. If WebKit gives up and closes, the stateUpdateHandler removes them on `.failed`/`.cancelled`. Does WebKit cancel the proxy connection on its own timeout? Yes eventually. But: parked clients with a long-lived TCP connection to the relay stay for the whole background period — iOS suspends the process anyway; on resume gate opens → `beginRelay` on a client that WebKit already abandoned → relay to upstream, upstream eventually errors, both cancelled. Minor resource churn. Also `handle(client:)` sets `client.stateUpdateHandler` AFTER `client.start(queue:)` — a state update could fire before the handler is installed? `client.start(queue: queue)` then `waitingClients[id] = client` then set handler — all on the same serial queue? No! `handle(client:)` is called from `newConnectionHandler` which fires on the listener's queue (queue) — so handle runs on `queue`, and client.stateUpdateHandler callbacks also fire on `queue` (client.start(queue: queue)) — serial, so no race. But there's a subtle one: in the held path, stateUpdateHandler is installed AFTER start; a `.failed` event could be delivered between start and handler installation → missed → client stays in waitingClients forever (leak). Events are delivered async on the same queue, and the handler is set synchronously within the same queue block, so the earliest callback can't interleave. Safe.

**setUpstreamAvailable(true) releasing waiting clients**: `beginRelay` sets `client.stateUpdateHandler = nil` — removing the cleanup handler. Fine since it's now relayed.

**stop()**: cancels listener + waiting clients on queue. Established relays (upstream conns) NOT tracked → NOT cancelled! `socksLogProxy?.stop()` in rebuild path leaves established relay pumps alive — they keep pumping to tsnet's OLD loopback listener (now closed via node.close()) → upstream errors → pump cancels both. Self-cleaning, but WebKit client connections to the relay die at that point with RST-ish behavior. OK.

**Bigger issue — WebKit retains old proxy config across node rebuild**: `rebuildNodeAfterFailedResume` stops the relay, sets node=nil, then `startTailscaleIfNeeded` → eventually `tailscaleUp` publishes a NEW proxyConfiguration (new loopback port, new credentials). Between relay.stop() and the new publish, `model.proxyConfiguration` still points at the DEAD relay port → WebKit loads fail -1000 during the rebuild window (waiting clients were cancelled by stop(), new ones get connection-refused on the dead listener port). Meanwhile `model.state`: background set .Starting; recovery set .Running after rebind... on the rebuild path, is state forced back to .Starting? `rebuildNodeAfterFailedResume` does NOT set model.state — it stays .Running (set by the recovery task before observer recovery). So the gate logic (`isConnected`) believes connected, the SOCKS relay is dead, WebKit requests to the dead port get connection refused → navigationError surfaces (isConnected=true → shows error overlay!) and — critically — `TSNetConsumer` stale... The new node start: `startTailscale` → setupNode → new node → tailscaleUp → startEventBus → eventually new initial state → state updates → proxyConfig published → BrowserViewModel.applyProxy installs it. During the gap (could be seconds — node start + localAPI), tabs error out. Also `proxyConfig(loopback)` → relay start blocks main up to 3s again. And — the published proxyConfig only happens if `!didEnterBackground, generation == activeObservationGeneration`... no wait, that's for the processor; the loopback publish is unconditional. OK.

**CRITICAL: rebuild during rebuild — `startInFlight` and double nodes**: `rebuildNodeAfterFailedResume` sets `startInFlight = false` then calls startTailscaleIfNeeded → startInFlight=true → Task → startTailscale → setupNode creates NEW TailscaleNode. Meanwhile the OLD TailscaleNode actor might still be executing `close()` — close is awaited BEFORE startTailscaleIfNeeded, so sequential. OK. But `tailscale_start` on a new server handle — TsnetNewServer each time; old sd removed from map by TsnetClose. If close() THROWS (s.s.Close() error — e.g., logtail shutdown error), TsnetClose already deleted from map; Swift throws, logged; new node starts anyway. Two tsnet Servers in the same process on the SAME state dir (old one not Closed — well TsnetClose calls s.s.Close() and THAT failed, so the Go server may be partially alive) → two magicsocks, state store conflicts, the OLD loopback listener still bound. Comment in tailscaleUp warns about "two tsnet servers on the same state dir". Edge case, HIGH impact when it happens (Go-level Close rarely errors though — it returns error from logtail? `Close()` aggregates... it can return err from lb.Shutdown? Actually tsnet's close() doesn't return error — `s.close()` returns nothing; `Close()` returns nil unless already closed. So `close()` throw ≈ impossible except double-close → EBADF. Downgrade: LOW likelihood.)

**startTailscale fatalError**: as noted, ANY error in startup → fatalError → CRASH. This includes rebuild-after-resume. If `TailscaleNode.init` throws (e.g., tailscale_start fails because the state dir is locked by a zombie old server, or control URL bad), the app deliberately crashes on a foreground resume — turning a recoverable network blip into an app crash + lost tabs (well, tabs persist). This is a HIGH finding: the "last resort" recovery path can crash the app. Also fatalError in a library-quality path is hostile: TestFlight users hitting rebuild get a crash report, not a re-login.

**`processor?.cancel()` in willEnterBackground then `model.state = .Starting`**: this SYNTHESIZES state — the UI gate uses `state == .Running`. With state=.Starting, `StatusViewModel.observeAuthURL` gets state != NeedsLogin → `authManager.cancel(reason:)` — CANCELS AN IN-FLIGHT ASWebAuthenticationSession! Scenario: user is mid-login (auth sheet open), presses home/locks screen → willEnterBackground sets state=.Starting → observeAuthURL else-branch → authManager.cancel → auth sheet dismissed → `authSessionEndedGeneration` bump, `requestedInteractiveLogin = false`. On resume, login flow is dead; user must tap Login again. Compare with the .inactive comment that explicitly warns about this exact bug class ("it recreated the node while an ASWebAuthenticationSession sheet was still open... silent login failure"). Here it's not node recreation but the synthetic state flip cancelling the sheet. Does it? `authManager.cancel` — need to check AuthManager... I didn't read AuthManager.swift. But the pattern strongly suggests backgrounding during interactive login aborts the login. Real-world: OAuth to control plane may take >30s; user switches to password manager app to copy credentials → background → sheet cancelled → login fails. HIGH usability bug. Actually wait — does iOS deliver .background while an ASWebAuthenticationSession is showing? The session is presented in-app (Safari view); switching apps backgrounds Aperture → yes. And the fix for them was .inactive handling, but .background still cancels. Also `willEnterBackground` sets state=.Starting unconditionally even at NeedsLogin. Then `running = false`, fine. The LoginBanner path: needsAuth... state .Starting → needsAuth=false. On resume recovery sets .Running. If user was NeedsLogin before background, after resume: bus initial state re-emits NeedsLogin + BrowseToURL... but the auth sheet was already cancelled. HIGH.

Also synthesizing `.Starting` on background while the node may actually be `NeedsLogin`: the status-poll fallback (`ipnState(fromBackendState:)`) is stopped in background; on foreground recovery, the synthetic .Running is set after rebind — **