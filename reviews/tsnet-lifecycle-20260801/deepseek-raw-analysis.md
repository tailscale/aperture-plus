Let me start by exploring the repository structure and understanding the codebase. This is a deep adversarial review task, so I need to be thorough. Let me begin by looking at the repository layout.

--- ANALYSIS CHUNK ---

Let me look at the key files. Start with TSNetManager.swift, SocksLogProxy.swift, TSNetModel.swift, Logging.swift.

--- ANALYSIS CHUNK ---

This is a big file. Let me now read SocksLogProxy.swift, TSNetModel.swift, Logging.swift, and the TailscaleKit files.

--- ANALYSIS CHUNK ---

Now let me read Logging.swift, then the TailscaleKit files: TailscaleNode.swift, LocalAPIClient.swift, MessageReader.swift, MessageProcessor.swift.

--- ANALYSIS CHUNK ---

Now let me read the LocalAPI files: LocalAPIClient.swift, MessageReader.swift, MessageProcessor.swift, and Types.swift.

--- ANALYSIS CHUNK ---

Now let me read the browser files: BrowserViewModel.swift, BrowserTab.swift, TabManager.swift, TabbedBrowserView.swift, and the workspace files.

--- ANALYSIS CHUNK ---

Now let me read the workspace files and the app entry point.

--- ANALYSIS CHUNK ---

Now let me look at the Go side: tailscale.go, tailscale.c, tailscale.h, and the git history from 5071bac to HEAD. Let me also look at the test scripts.

--- ANALYSIS CHUNK ---

Now let me look at the git history around the relevant commits, and check the test scripts and UI tests. Let me also check the pinned go.mod version of tailscale.com.

--- ANALYSIS CHUNK ---

Let me now look at the UI tests to understand the test coverage, and then dig into the submodule's tailscale.com source for the key API semantics (DebugAction rebind/break-derp, Loopback, tsnet Server behavior). Let me also check the submodule commit and look at the tsnet package there.

--- ANALYSIS CHUNK ---

Now let me look at the tsnet source in the go module cache for key semantics, and check the LocalClient DebugAction implementation. Also check the tsnet Server Loopback and Close behavior, and whether Up blocks.

--- ANALYSIS CHUNK ---

Now I have a good picture of the tsnet semantics. Key observations so far:

1. `tsnet.Server.Up()` — waits until Running via WatchIPNBus. The app's comment says `Up` blocks until Running and that it would block the actor indefinitely for a NeedsLogin node. This is a documented concern. Note: `Up` is NOT actually called by the app anymore (deliberately). Also importantly, `Up` clears serve config and service advertisements on first run — skipping it means those don't happen, but that's not relevant here.

2. `LocalClient()` calls `s.Start()` if not started. Note the important nuance: `s.s.Start()` in the C wrapper only does `s.initOnce.Do(s.doInit)` — the comment says "Optional: any calls to Dial/Listen will also call Start". So `tailscale_start` only runs `doInit` → `start()` — it does NOT wait for Running and does NOT set WantRunning... Wait, actually let me check `tsnet.Server.start()` — whether it sets WantRunning. Let me look.

3. `TsnetDebugResetConnections` — `lc.DebugAction(ctx, "rebind")` then `"break-derp-conns"`. Need to verify that these action strings are valid — the local API has `debugAction` at /localapi/v0/debug?action=... Let me check the localapi server code for the action names.

4. `tsnet.Close` — closeOnce; double close returns error. The Swift `deinit` calls `tailscale_close` — and if the app already called `close()` explicitly (via rebuildNodeAfterFailedResume), the deinit will call it again → returns error but that's fine (EBADF? No — the server is still in the map; the second close returns `net.ErrClosed` error → -1; actually wait: `TsnetClose` deletes from servers map at the beginning, so a second call finds s == nil → EBADF. But there's a race: `getServer` etc. OK.

Wait, actually there's a subtle bug in `TsnetClose`: it deletes the server from the map BEFORE calling `s.s.Close()`. If `Close()` fails, the server is gone from the map but the tsnet server may still be alive, leaking. Also `deinit` on TailscaleNode will call `tailscale_close` again → EBADF → fine.

But the more important issue: the `deinit` of TailscaleNode calls `tailscale_close(tailscale)` synchronously — on whatever thread deinits the actor. When `rebuildNodeAfterFailedResume` does `self.node = nil` after `try await node.close()`, deinit runs and calls `tailscale_close` again — the Go side has already deleted the server from the map (in the first close), so EBADF, no-op. OK.

But wait — there's a serious concern: `TailscaleNode` is an actor; `deinit` of an actor is nonisolated and runs synchronously. `tailscale_close` calls into Go `TsnetClose` which locks `servers.mu` and then calls `s.s.Close()` which does a LOT of work synchronously — including `lb.Shutdown()` which can block. The comment says "It must not be called before or concurrently with Start". But the actual close is called from `close()` (async) which is awaited. However, in the initial `startTailscale` failure path: `fatalError` after `startInFlight = false` — that's process death anyway.

Now — a big one: In `TSNetManager.willEnterBackground`, the code does NOT close the node; keeps it running. Good.

Let me now check `tsnet.Server.start()` to see whether `tailscale_start` sets WantRunning and starts LoginInteractive. And check what happens on `Start` when it's already running.

Also check `TsnetStart` — `s.s.Start()` — `doInit` runs `s.start()`. Let me look at `start()`.

--- ANALYSIS CHUNK ---

Good. Now I understand:

- `TsnetStart` → `s.s.Start()` → `doInit` → `start()` — sets WantRunning=true, starts backend, StartLoginInteractive if NeedsLogin. So the app's assertion is right: `tailscale_start` begins login interactively. `Up` is redundant (except it clears serve config etc.).

- `DebugAction("rebind")` → `h.b.DebugRebind()` → magicsock rebind; `"break-derp-conns"` → `DebugBreakDERPConns()`. Both are synchronous. `PermitWrite` is true in tsnet (lah.PermitWrite = true). Good — the app's use is valid.

Now, key question for the review: `lc.DebugAction(ctx, ...)` — does it use `PermitWrite`? There's a subtlety: for tsnet's in-process local client, `PermitWrite = true`. OK.

Now let me check `DebugRebind` and `DebugBreakDERPConns` semantics — do they preserve existing netstack TCP sessions? `DebugBreakDERPConns` breaks DERP connections; existing TCP sessions over the same DERP will fail and be torn down; but a TCP session over a direct connection survives. `DebugRebind` closes UDP sockets — any session currently using UDP (direct) will break. Existing sessions over DERP that get broken will error. So "live netstack TCP sessions actually survive/recover" — they survive if they were on a different path than what was broken; TCP sessions over the broken path die. HTTP connections over the SOCKS proxy are typically short-lived, but long-poll WebSocket or streaming could die. WKWebView keeps HTTP/2 connections in the pool — those would die and WebKit reconnects transparently.

Now — the deep questions for the review. Let me methodically trace the concrete operation sequences and races:

### 1. Deadlocks/hangs/crashes

**A. `startTailscale()` failure → `fatalError`.** In `startTailscale`, if `setupNode()` or `LocalAPIClient` init or `tailscaleUp` throws, it calls `fatalError("Error setting up Tailscale: \(error)")`. Note: it sets `startInFlight = false` before fatalError — irrelevant since fatalError kills the process. But fatalError on a throw path — e.g., if `watchIPNBus` throws because the loopback isn't ready — kills the whole app. This is a real crash vector: `tailscaleUp` → `startEventBus` → `localAPI.watchIPNBus` → `basicAuthURLRequest` → `URLSessionConfiguration.tailscaleSession(node)` → `node.loopback()`. Let me check `URLSessionConfiguration.tailscaleSession` — it calls `node.loopback()` and possibly `node.addrs()`. If `loopback()` throws (e.g., loopback listener not ready right after Start? No — Loopback calls Start and sets up listener synchronously), the whole app crashes. When can watchIPNBus throw? `basicAuthURLRequest` errors on URL generation. Rare.

More important: **startTailscale crash on second launch of a workspace**: `setupNode` guards `self.node == nil`. If a previous node exists (e.g., fallback rebuild started a new node via `startTailscaleIfNeeded`), and `startTailscale` runs again... wait, `startInFlight` guard prevents concurrent starts, but `startTailscaleIfNeeded` sets `startInFlight = true` then spawns Task; if `startTailscale` throws → fatalError. Hmm.

**B. The `fatalError` in `startTailscale` is on the whole app.** `try await MainActor.run { try setupNode() }` — setupNode throws only if `TailscaleNode.init` throws (tailscale_new returns nil or start fails). A transient start failure (e.g., state dir locked by another tsnet instance from a previous crash, or disk full) → **the entire app dies on launch**. That's a crash-by-design on startup failure. Worth flagging: after `rebuildNodeAfterFailedResume` → `startTailscaleIfNeeded` → new node → `startTailscale` → if the rebuild's `tailscaleUp` throws (bus watcher can't start), fatalError → app crash instead of retry. Wait — in rebuildNodeAfterFailedResume, `startTailscaleIfNeeded` is called which spawns a Task running `startTailscale`, which on error → fatalError. So the "bounded node-rebuild fallback" that is supposed to keep the app alive ends in a fatalError if the new node's setup throws. Also `startInFlight` gets reset to false before fatalError.

Also the initial `startTailscale` — the catch resets `startInFlight` and fatalErrors. The comment in the code about double-start: `startInFlight` guard. But `willEnterForeground` → `startTailscaleIfNeeded` → if `didEnterBackground` true and node == nil. Hmm.

**C. `TailscaleNode` actor + synchronous C calls.** All `tailscale_*` calls run synchronously on the actor's executor. `up()` is deliberately not called (good). `loopback()` is called from `tailscaleUp`. `close()` called during `rebuildNodeAfterFailedResume`. These are fine because the actor executor is a serial executor — but `up()` would block it; the app avoided that.

BUT: `deinit { tailscale_close(tailscale) }` — actor deinit runs on a nonisolated context, calling into Go. If a `close()` was already awaited, `TsnetClose` second call → EBADF (removed from map) → fine.

However, there's a race: **`TailscaleNode` deinit can run while Go is mid-`Close()`** — no, close is awaited before `self.node = nil`.

What about `node` never being set to nil except in `rebuildNodeAfterFailedResume`? So normally deinit only happens at process death. OK.

**D. `scheduleBusRestart` — recursion/ownership.** `busErrorWatcher` is a `Combine` sink on `consumer.$error`. The consumer's `error(_:)` sets `self.error = error` on MainActor. The sink fires → `scheduleBusRestart` → cancels previous `busRestartTask`, clears `consumer.error = nil`, creates new Task. In the Task: loop, call `startEventBus` — which itself creates a NEW `busObserver` sink on `consumer.$error` (wait — in `startEventBus`, the sink is `await consumer.$error.sink { ... }` — the consumer is the same object passed in; in recovery the consumer is new, but in the restart path, the consumer is the SAME consumer whose error triggered the restart). Hmm, careful:

- In `startEventBus`, the code does `await consumer.$error.sink { ... }` — the `await` is needed because `$error` is a `@MainActor @Published`. The sink is created with `[weak self]` — captures self (TSNetManager). Then on MainActor, `busErrorWatcher?.cancel(); busErrorWatcher = busObserver` if generation matches.

Race: the sink could fire between `await consumer.$error.sink` and the MainActor.run block that installs it — it would call `scheduleBusRestart` on the old generation → guarded by `generation == model.activeObservationGeneration` → for the same generation, it could schedule a restart while the new processor is being set up. Then `scheduleBusRestart` cancels `busRestartTask` and starts a NEW restart task that also calls `startEventBus` with the same generation... This can create TWO concurrent bus watchers for the same generation. Then `setProcessor` is called by whichever finishes last, canceling the other's processor. Actually `setProcessor` cancels the previous processor. But both restart tasks could each create processors and each call `setProcessor` — the second `setProcessor` cancels the first processor. And the `busRestartTask` variable holds only the last task; the first task continues (it wasn't cancelled — only `busRestartTask?.cancel()` cancels the stored one... wait, in the flow: `busErrorWatcher` fires → `scheduleBusRestart` → `busRestartTask?.cancel(); busRestartTask = Task {...}`. If two sinks fire, the second cancels the first task — but only if the first task is stored in `busRestartTask` when the second fires. Since `busRestartTask` is set synchronously in `scheduleBusRestart` (MainActor), yes the second cancels the first. But what if the first task already passed its `guard !Task.isCancelled` check and is awaiting `startEventBus`? Cancellation doesn't interrupt the await of `watchIPNBus` (URLSession call) — actually `Task.isCancelled` would be checked after the await. `watchIPNBus` is async and cancellation... `session.data(for:)` is not cancellation-aware by default (it uses the old async API which does not propagate Task cancellation). So the canceled task can still complete its URLSession setup and then call `setProcessor` — canceling the NEW processor! Sequence:

1. Task A (restart 1) created, awaiting startEventBus.
2. Error fires again (consumer.error set again by new processor's error callback?) → scheduleBusRestart → cancels Task A, starts Task B.
3. Task A completes startEventBus (its URLSession got created; the `await consumer.$error.sink` inside startEventBus... wait the sink creation requires MainActor hop; cancellation checked at `guard !Task.isCancelled` in the Task... Actually Task A's code: `let processor = try await self.startEventBus(...)`; after await, `guard !Task.isCancelled, !self.didEnterBackground, generation == ...` — Task A IS cancelled → `processor.cancel(); return`. Good, the guard catches it.

But there's a subtle window: Task A's `startEventBus` installs ITS OWN busObserver into `busErrorWatcher` (on MainActor). Then Task A's post-await guard sees Task.isCancelled → cancels processor → but the installed `busErrorWatcher` is Task A's observer, which is still live and pointing at the same consumer... then Task B's startEventBus will replace it. OK, mostly handled.

But here's a real issue: **the `error` on the consumer is only cleared by `scheduleBusRestart` (`consumer.error = nil`)**. If `busErrorWatcher` sink fires while `busRestartTask` is still running (between cancel and new start), the restart task loop's `startEventBus` will attach a new sink. Fine.

Another real issue: **`startEventBus`'s busObserver sink fires for ANY consumer error, including errors from the status poll or other endpoints?** No — `consumer.error(_:)` is only called by `MessageProcessor.processError` (from `MessageReader.urlSession didCompleteWithError`). OK.

**E. `MessageReader` congestion & drain.** kMaxQueueSize = 24. If congested, `receiveData` sets congested=true and signals. The drain() clears congested and calls errorHandler with `.queueCongested` → `processError` → consumer.error → bus restart with `.initialState`. Wait — the drain's errorHandler is the one passed to `reader.start` — which is `errorHandler` param or the default that routes to `processError`. In `MessageProcessor.start`, `errorHandler` = the one passed (nil by default from watchIPNBus) → default: `self?.processError(error)` → consumer.error → TSNetManager restart. That's the intended restart-on-congestion path. OK.

But note: in `MessageReader.drain`, if congested, errorHandler is invoked AFTER `completion(messages)` — the completion processes the drained messages first. But then errorHandler fires → scheduleBusRestart → cancels busRestartTask → new processor. Meanwhile the drained messages were already processed (state events) — fine.

**F. `MessageReader.stop()` vs `start()` racing on the same OperationQueue.** Both enqueue operations on workQueue; serial; OK. But `MessageProcessor.cancel()` calls `reader.stop()` — which enqueues. `MessageProcessor.start()` enqueues `cancel()` + `reader.start(...)`. There's a subtle ordering issue in `MessageProcessor.start`: it enqueues on ITS OWN workQueue (processor.workQueue) an operation that calls `reader.start`. But `messagesAvailable()` and `drain()` are invoked from the reader's workQueue (URLSession delegate queue). The `messagesAvailable()` → `reader.drain` → adds operation to reader's queue → OK. But `processMessage` adds to processor's workQueue. Fine.

Hmm wait, one potential deadlock: `MessageProcessor.start` runs `cancel()` (which calls `reader.stop()` → enqueues on reader's queue) INSIDE an operation on processor.workQueue. `reader.stop()` just enqueues — doesn't wait. OK no deadlock.

`cancel()` sets `cancelled=true` under stateLock, then `reader.stop()` enqueues invalidation. But `messagesAvailable` checks `cancelled` under stateLock — after cancel, no drain. Good.

**G. URLSession creation on reader queue while another URLSession being invalidated** — handled by serial queue. But: `MessageProcessor.start` can be called concurrently from `startEventBus` (from `scheduleBusRestart` Task and `recoverLocalAPIObserversAfterResume` loop). Two `MessageProcessor` instances created (each with own reader). The old processor's `cancel()` invalidates its session. But `startEventBus` creates a NEW MessageProcessor each time (via `watchIPNBus` → `MessageProcessor(consumer:)` → `messageProcessor.start(...)`). The old processor is cancelled via `setProcessor` (cancels previous). But the old processor's URLSession invalidation happens on ITS reader queue — no shared state with the new one. OK.

**H. `TSNetConsumer` actor — `notify` is actor-isolated; each `notify` spawns a Task to MainActor.** MessageProcessor.processMessage does `Task { await consumer.notify(notify) }` from the processor's workQueue. Each message is a separate Task — unbounded concurrency on the consumer actor is fine (serialized), but the MainActor hop per message could reorder? No — Tasks are FIFO scheduled but `Task { @MainActor in ... }` from the actor: ordering is not guaranteed across multiple Tasks! `notify` is async (actor method); the inner `Task { @MainActor ... }` is unstructured; multiple such Tasks hop to MainActor — they are scheduled in creation order on the cooperative pool and enqueued to MainActor in order... In practice, unstructured Tasks created in quick succession from the same actor are scheduled in order, but this is NOT a strict guarantee (Task ordering is not guaranteed when tasks are created on different executors or when the cooperative pool reorders). Actually within one actor executor, tasks are enqueued in order, and the `Task {}` blocks are created synchronously one after another on that actor... the creation order is deterministic, and MainActor enqueues in creation order. Practically ordered. But `freshLocalAPIResponseGeneration` etc. is not a big deal.

The stale-generation guard: `model.activeObservationGeneration == self.observationGeneration`. TSNetConsumer is created with observationGeneration; if generation mismatch → discard. Good.

**I. Status poll — `startStatusPolling` guard `statusPollTask == nil`.** In `setLocalAPIClient`, `if !didEnterBackground { startStatusPolling() }`. But `startStatusPolling()` is @MainActor and its Task checks `if let client = await MainActor.run(body: { self.localAPIClient })`. Note: the Task captures `self` strongly (Task { [weak self] ... } — actually it's `Task { [weak self] in while ... guard let self else { return } ...`). It loops forever with 5s sleeps, checking Task.isCancelled. The expectedGeneration check: `guard expectedGeneration == self.model.activeObservationGeneration, !self.didEnterBackground else { return }` — it RETURNS (not continue) — so if a stale generation poll happens to run after a generation bump, it exits the loop. But it also doesn't cancel the task; it just returns from inside the loop... wait, `return` inside the `while` loop body returns from the Task closure — terminating the poll loop. But `statusPollTask` still holds the finished task; a new `startStatusPolling` would see `statusPollTask != nil` and NOT start a new one. Hmm: in `recoverLocalAPIObserversAfterResume`, `stopStatusPolling()` is called first (`statusPollTask?.cancel(); statusPollTask = nil`), then `startStatusPolling(generation:)`. But the old task's `return` guard exits the loop silently. OK.

Actually wait — there's a subtle bug: the status poll Task uses `Task { [weak self] in while !Task.isCancelled { guard let self else { return } ... try? await Task.sleep(nanoseconds: 5s) } }`. The guard `expectedGeneration == ... else { return }` is inside the `while`, so on generation mismatch it returns — but the `while !Task.isCancelled` condition was checked at the top; the return exits. Fine.

But: **two status poll tasks could coexist**: `startStatusPolling` guards `statusPollTask == nil`. When is it set to nil? `stopStatusPolling`. In `recoverLocalAPIObserversAfterResume` loop: `stopStatusPolling(); ... startStatusPolling(generation: generation)`. Since the loop is sequential on MainActor, no overlap. But there's the initial startup path: `setLocalAPIClient` → `startStatusPolling()` (no generation arg → uses `model.activeObservationGeneration`). And `tailscaleUp` may be running concurrently... the first call to `setLocalAPIClient` happens in `startTailscale`. Then `startStatusPolling` in `setLocalAPIClient`. If background happens between, `stopStatusPolling` cancels. On foreground, `recoverLocalAPIObserversAfterResume` restarts. OK.

**J. `willEnterBackground` sets `model.state = .Starting`** — this makes BrowserViewModel's `$state` sink fire → `applyConnectionState(false)` → isConnected=false → banner. Also `proxyAvailabilityWatcher` → `socksLogProxy.setUpstreamAvailable(false)` — gate closed. And BrowserViewModel's proxyConfiguration sink is NOT triggered (proxyConfiguration unchanged) → WebKit proxy config retained. Good.

But — **the `model.$state` sink in BrowserViewModel with `.removeDuplicates()`**: `.map { $0 == .Running }.removeDuplicates()` — this operates on the publisher pipeline... `removeDuplicates` on Bool. Fine.

**K. WebKit WKWebsiteDataStore + proxyConfigurations mutation while pages are loading.** `applyProxy` sets `dataStore.proxyConfigurations = [proxy]` — this can tear down WebKit connection pools, causing in-flight loads to fail (they'd get -1000 or -1005). The code claims no reload happens; but a page mid-load during a proxy policy update (e.g., `refreshProxyPolicyIfNeeded` republishing matchDomains every 5s poll when policy changed) → connection pool teardown → the page's ongoing XHR/fetch fails; the test `testConnectionBounceDoesNotReloadPageOrLoseFetch` asserts a fetch survives a state bounce (not a policy change). A `matchDomains` change (new peer joins) republishes the proxy — WebKit may restart the data store networking. Whether that resets in-flight requests is a real question. This is a medium finding.

**L. `SocksLogProxy` waiting clients & half-close.** Let me trace:

- `handle(client:)`: if `!upstreamAvailable` → store in `waitingClients`, set stateUpdateHandler. When `.cancelled` → remove. When `.failed` → remove. When gate opens (`setUpstreamAvailable(true)`) → `beginRelay` for all waiting. Note: **`beginRelay` sets `client.stateUpdateHandler = nil` — but if the client had been cancelled in between, we'd call beginRelay on a cancelled connection** — sending on a cancelled NWConnection throws/errors; `pump` from cancelled conn → receive returns error → `from.cancel(); to.cancel()`. Handled gracefully.

- Waiting clients: **no timeout**. A client can wait forever if tsnet never returns to Running (e.g., node dead, or gate never reopens because model.state never returns to .Running — e.g., during a 30s observer-recovery). WebKit's request has its own timeout (default 60s per NSURLRequest) — eventually WebKit aborts. But the client connection stays in the dict forever if WebKit never cancels (e.g., HTTP keep-alive connections held by CFNetwork's pool — those are long-lived and CAN wait indefinitely, holding an fd + memory). Actually, does WebKit keep persistent SOCKS connections? CFNetwork multiplexes HTTP/2 over one TCP connection per origin; that connection is long-lived (keep-alive ~ minutes). If the app is in a state where the gate stays closed for a long time (e.g., `model.state` stuck at `.Starting` because the IPN bus is dead and the status poll is also dead — the "permanent reconnect" scenario), those waiting clients pile up. When the node is rebuilt (`rebuildNodeAfterFailedResume` → `socksLogProxy?.stop()` cancels all waiting clients) — good, that's the bounded fallback. But between gate-closed and node-rebuild (up to ~3 attempts * (4s + 1s) + ...) — bounded. The waiting list is only drained on gate open or stop. It's unbounded during a permanent stuck state, but the node-rebuild fallback bounds it to ~15-20s. Hmm, but wait: `rebuildNodeAfterFailedResume` only runs if `recoverLocalAPIObserversAfterResume` reaches attempt >= 3. That loop requires the Task to not be cancelled and generation matches. OK, bounded ~ (4s + 1s) * 3 ≈ 15s + startEventBus overhead. OK.

**M. `pump` recursion.** `pump` uses recursion via the completion handler: `to.send(content:, completion: .contentProcessed { _ in self?.pump(...) })`. Each send completion spawns the next receive. `contentProcessed` means the send completed to the OS buffer (not end-to-end). This is a recursive chain but asynchronous — no stack growth. But **backpressure**: `pump` reads from `from` only after the previous send completes — so there is backpressure on the read side. However, `send` with `.contentProcessed` returns when the data is processed by the connection, not when it's delivered; if `to` (upstream) is slow, the send completion may still be quick (buffered), so `pump` keeps reading — memory could grow? Each receive is at most 64KB; the chain is serialized (one receive at a time per direction). So bounded. OK.

**N. Half-close handling**: When client sends FIN (isComplete), `from.cancel(); to.cancel()` — cancels BOTH directions immediately. In SOCKS5 CONNECT, when the client half-closes (e.g., HTTP request finished, waiting for response), the server should keep the upstream->client direction alive. NWConnection `cancel()` tears down the whole connection. So a client that half-closes its write side (which browsers do NOT normally do for HTTP, but some protocols do; WebSocket? No; but curl/streaming clients might) → the relay kills the upstream too → data loss. For WKWebView HTTP traffic, the client (CFNetwork) does not half-close; it sends requests and waits. So low severity, but a correctness bug for half-close semantics. Also when `isComplete` on `from` → cancel both: **the other direction's in-flight data is lost**. E.g., the SOCKS server reply arrives after client half-close → lost. Real-world impact: minor for HTTP.

Also `error != nil` → cancel both: a transient upstream reset (tsnet reconnect breaks the loopback proxy's upstream session? No — the relay connects to tsnet's loopback listener, which persists across the node's transport rebuild) kills the client connection. When tsnet's loopback proxy resets an in-flight dial (e.g., break-derp kills a session mid-dial), the upstream connection sends RST → relay sees error → cancels client → WebKit gets a SOCKS failure → -1000 → error overlay! Wait — but `navigationError` is suppressed when `!isConnected`. Hmm, but during the connection-reset test, `model.state = .Starting` first → isConnected=false → navigation errors suppressed → then state=Running → the -1000 from a killed session... The window where a page is mid-load during the reset: the reset breaks DERP; in-flight dials fail with "connection reset"; the SOCKS reply is REP 0x01/0x05 (general failure / connection refused?) → relay logs FAILED → WebKit reports -1000 → navigationError → but isConnected at that moment? In `runConnectionResetTestIfNeeded`: `model.state = .Starting` (isConnected=false) → wait 2s → debugResetConnections → wait 2s → `model.state = .Running`. During the 2s Starting window, in-flight page fetches would fail with -1000 but the error is suppressed because `!isConnected`. When state flips back to Running, the page may show partial content; a subsequent retry works. The test asserts the document didn't reload (loads count stays 1) and no error overlay. If a navigation was mid-flight at reset time → didFailProvisionalNavigation fires with isConnected==false → suppressed → no error overlay, no reload. Then didCommit already happened? For a page already committed, subsequent fetches failing don't trigger didFail* (they're XHR). OK, plausible.

**O. The `runConnectionResetTestIfNeeded` vs foreground recovery**: `didRunConnectionResetTest` guard — runs once. Fine.

**P. `startEventBus` generation-race on initial startup**: `tailscaleUp` computes `generation = model.activeObservationGeneration` (0 initially). If `didEnterBackground` became true while awaiting, `setProcessor` is skipped and processor cancelled — "Startup observers completed after background; foreground recovery owns restart". But then **nothing starts the LocalAPI observers again until `willEnterForeground`** — which requires `didEnterBackground` to be true (it is) → recovery runs. But what if the app went background → `willEnterBackground` ran (didEnterBackground=true, generation bump to 1) → then startup's `startEventBus` returns AFTER background → processor cancelled, `setProcessor` skipped. Then the app goes foreground → `willEnterForeground` → `recoverLocalAPIObserversAfterResume` with generation 1. But wait — **the `statusPollTask`**: `setLocalAPIClient` ran `if !didEnterBackground { startStatusPolling() }` — that happened before background (during startup) → statusPollTask running with expectedGeneration 0. `willEnterBackground` → `stopStatusPolling()` cancels it. OK.

But there's the `localAPIClient`/`node` interplay: recovery requires `self.node` non-nil. If startup completed the `loopback()` publish before background (tailscaleUp publishes proxy config even when cancelled — "Continue to publish the stable loopback proxy below"), then WebKit has a proxy; the gate holds new connections until Running. Recovery reconnects observers. If node setup FAILED and `startTailscale` → fatalError → crash. If node == nil and willEnterForeground → `startTailscaleIfNeeded()`. OK.

**Q. Multiple rapid background/foreground transitions.** `willEnterBackground` bumps `foregroundRecoveryGeneration &+= 1` and cancels `foregroundRecoveryTask`. `willEnterForeground` → new task with current generation. In-flight old task's guards check `generation == self.foregroundRecoveryGeneration` — old task sees mismatch → returns. But: `recoverLocalAPIObserversAfterResume`'s while loop condition includes `generation == foregroundRecoveryGeneration` — old generation tasks exit. Good.

**But wait — `willEnterBackground` while `recoverLocalAPIObserversAfterResume` is awaiting `startEventBus` or inside the `while` loop**: the loop checks `!didEnterBackground` → exits. But it might have already created a NEW consumer and called `startEventBus`, and `setProcessor(newProcessor)` + `startStatusPolling` — those happen under MainActor. `willEnterBackground` (MainActor) runs after; cancels processor. But between the loop's `startStatusPolling(generation: generation)` and the background's `stopStatusPolling`, could the loop's `startStatusPolling` be blocked? No. Order fine.

**R. Unowned self in `recoverLocalAPIObserversAfterResume`** — it's `[weak self]` in the Task; `await self.recoverLocalAPIObserversAfterResume` — inside, `self` is strongly retained for the duration. OK.

**S. `debugResetConnections` — synchronous C call on a Swift Task**: The Go side has a 5s timeout, so the Task awaits ≤5s. But **the synchronous call blocks the thread** — Swift Tasks run on the cooperative thread pool; a 5s blocking call is tolerable but blocks a pool thread. The actor: `await node.debugResetConnections()` hops to the TailscaleNode actor — the actor's executor is a shared thread pool. OK.

**T. `willEnterForeground` sets `model.state = .Running` optimistically after debugResetConnections succeeds** — even if the IPN bus hasn't confirmed Running. If the node is actually in NeedsLogin (user logged out while backgrounded — not possible) or the transport is broken such that control connection is down, the gate opens but the proxy can't dial → -1000 errors suppressed? isConnected=true → navigation errors are NOT suppressed → user sees error overlay. Hmm — opening the gate without bus evidence of Running can produce a brief window of false "Connected" with failing loads. But debugResetConnections does reach LocalBackend and requires the backend to respond to the debug action — that implies the backend is up. The Go call succeeding means the backend processed rebind + break-derp. The state could still be NeedsLogin (if user logged out). In the normal flow, the bus recovery would soon correct the state. Minor.

**U. `Consumer` swap in recovery: `consumer = newConsumer` while old consumer's in-flight `notify` Tasks still hop to MainActor** — the stale guard uses `observationGeneration`; new consumer has the new generation → old consumer's Tasks see mismatch → discarded. But `consumer` property is also used by `startEventBus`'s sink closure (`consumer.$error`)... `busErrorWatcher` is replaced in the new `startEventBus`. The old consumer's `error` sink: was installed in old `startEventBus` — wait, the old processor's consumer was the OLD consumer; its `busErrorWatcher` sink pointed at old consumer.$error with `scheduleBusRestart(localAPI:consumer:generation:error:)` capturing the OLD consumer and generation — but that sink is cancelled in `startEventBus`'s MainActor block (busErrorWatcher?.cancel()). Between cancellation and any late error... fine.

**V. What about `TSNetManager.node` retention across rebuild**: `rebuildNodeAfterFailedResume` sets `self.node = nil` AFTER `try await node.close()` — between `node.close()` and `self.node = nil`, the actor still holds the old node; new start creates new TailscaleNode. Note the OLD node's deinit will call tailscale_close again (EBADF, no-op). OK.

**W. Proxy config credential: `applyCredential` on `ProxyConfiguration`** — matches tsnet's `proxyCred`. If the node is REBUILT, the loopback credentials CHANGE (new random cred per Loopback call — `s.proxyCred` is only generated once per Server: `if s.loopbackListener == nil` — cred generated once per Server lifetime). On node rebuild, new creds. `rebuildNodeAfterFailedResume` → `startTailscaleIfNeeded` → `tailscaleUp` → `loopback()` → new proxyConfig with new creds → published to model.proxyConfiguration → BrowserViewModel.applyProxy → `dataStore.proxyConfigurations = [proxy]` — replaces config including credentials. **If WebKit has existing pooled connections through the OLD proxy config (old creds), does replacing proxyConfigurations invalidate them?** The pooled connections were to 127.0.0.1:<old socksLogProxyPort> — and `rebuildNodeAfterFailedResume` STOPS the socksLogProxy and clears socksLogProxyPort, then `proxyConfig` creates a NEW relay (new local port) and a new `proxyHost` 127.0.0.1:newport. So pooled connections to the old port hit... nothing (listener stopped) → RST → new connections via new config. WebKit should create new connections for new requests. In-flight ones fail. Reasonable.

But WAIT — the socksLogProxy `stop()` is `queue.async` — asynchronous. `proxyConfig` may then create a NEW SocksLogProxy with the same port? `NWListener` with `allowLocalEndpointReuse = true` — the new listener binds to 127.0.0.1:0 (OS-assigned), so no conflict. But the OLD listener's `cancel()` is async on the old queue — the old queue may still process the cancel after the new listener started. `allowLocalEndpointReuse` means the old socket lingers in TIME_WAIT — new listener uses a fresh port. Fine.

**X. `SocksLogProxy` listener start uses a 3s semaphore wait — called from `tailscaleUp` on the TailscaleNode actor** (via `self.node?.loopback()` — wait no, proxyConfig is called from `tailscaleUp` after `await MainActor.run { model.proxyConfiguration = proxyConfig(loopback) }` — this runs on MainActor. `relay.start()` blocks MainActor for up to 3s! **Blocking the main thread up to 3 seconds** — on every startup and every node rebuild. NWListener on loopback typically becomes ready in ms, so usually fast, but under load it could stall the UI. Also `DispatchSemaphore` wait on MainActor = main thread stall → watchdog? 3s < 4s watchdog-ish, but a UI freeze. MEDIUM.

Wait — actually is `proxyConfig` called on MainActor? `await MainActor.run { model.proxyConfiguration = proxyConfig(loopback) }` — yes, MainActor. And `proxyConfig` → `relay.start()` → semaphore wait. Main thread blocked up to 3s. Yes — finding.

**Y. `setUpstreamAvailable` initial value**: `socksLogProxy?.setUpstreamAvailable(model.state == .Running)` — at proxyConfig time, state may be NeedsLogin/Starting → gate closed → OK.

**Z. Double `SocksLogProxy` on repeated `proxyConfig` calls**: `proxyConfig` is called in `tailscaleUp` and also `refreshProxyPolicyIfNeeded` doesn't call it. But `tailscaleUp` runs once per generation (startup + node rebuild). `socksLogProxy == nil` guard: after a rebuild, `socksLogProxy` was set to nil in `rebuildNodeAfterFailedResume`. OK. But on FIRST startup: `tailscaleUp` → proxyConfig → relay created. Then if the app is backgrounded and returns and recovery does `recoverLocalAPIObserversAfterResume` — doesn't touch the relay. Only the 3-attempt-failure path rebuilds. OK.

Now let me think about the "tabs sent home" question. Every path that can reset a tab to initial URL or recreate a webview:

1. `BrowserViewModel.loadInitial` — `didLoadInitial` guard; initial load happens when `makeWebView` is called and no pendingLoadURL. `unloadWebView` sets `pendingLoadURL = url` (the validated address-bar URL) — so on recreation, it loads the pending URL, NOT initial. Unless `url` is nil (never committed anything) → pendingLoadURL stays nil → `loadInitial()` → **initial URL**. So a tab that was created but never committed (e.g., mid-provisional-navigation when unloaded) returns to home when reselected. Also a tab whose `url` is the initial URL would reload

--- ANALYSIS CHUNK ---

The user wants me to write the complete review now without more tool calls. Let me synthesize everything I've learned. Let me structure the review carefully per the requested format:

1. Executive assessment (5-10 lines)
2. Findings ranked CRITICAL/HIGH/MEDIUM/LOW with file:function/line, concrete trigger sequence, impact, why tests miss it, concise fix
3. Dedicated "tabs sent home" causal analysis
4. Dedicated logging/observability audit
5. Prioritized test matrix
6. Things checked that are NOT bugs

Let me collect all my key observations:

**Key architecture facts established:**

- iOS 26, Swift 6 strict concurrency, `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`, so everything is implicitly MainActor.
- TSNetManager is @MainActor, one per workspace. Holds node, localAPIClient, processor, consumer, statusPollTask, socksLogProxy.
- TailscaleNode is an actor wrapping C calls (tailscale_*).
- LocalAPIClient is an actor creating one URLSession per request (`URLSession(configuration:)` per call, no session reuse!).
- MessageReader: URLSessionDataDelegate on a serial OperationQueue; newline-delimited JSON; kMaxQueueSize=24; congestion handling.
- MessageProcessor: serial queue, edge-triggered drain via messagesAvailableHandler.
- TSNetConsumer: actor implementing MessageConsumer, generation-tagged.
- SocksLogProxy: NWListener on 127.0.0.1 with a serial dispatch queue, passes bytes verbatim, parses copies, "holding" gate for waiting clients.
- Go side: tsnet.Server per sd; `TsnetStart` = Start() = doInit → start() which sets WantRunning=true, StartLoginInteractive if needed; `TsnetUp` = Up() blocking until Running.
- `TsnetDebugResetConnections`: LocalClient → DebugAction "rebind" + "break-derp-conns" with 5s ctx timeout.
- `TsnetClose`: deletes server from map, then Close(). closeOnce in Go — double close returns net.ErrClosed error (-1). Swift deinit also calls tailscale_close → EBADF no-op.
- go.mod pins tailscale.com v1.103.0-pre.0.20260728173411-71e90b2a2fe4 (post-v1.102 mainline with TS_FORCE_CACHE_NETMAP patch).

**Now let me trace the concrete bug candidates:**

### Finding 1 (CRITICAL/HIGH): `willEnterForeground` uses `debugResetConnections` unconditionally on EVERY foreground resume — including resumes where the app never actually suspended its sockets (rapid bg/fg, app-switcher, notification center), and regardless of whether the node is Running. Actually — wait, the code: `guard didEnterBackground else { return }`. So only after real .background. But: `.inactive` does nothing, and .background→.active always fires. On iOS, after .background, the process is typically suspended shortly after; but there's a window where the app went background but was NOT suspended (e.g., App Library, quick return, or a backgrounded app that stays running because of background tasks). In that case, `debugResetConnections` breaks DERP conns + rebinds magicsock unnecessarily — this kills live netstack TCP sessions? No — DebugRebind and DebugBreakDERPConns... Let me think about whether live TCP sessions survive.

DebugBreakDERPConns: breaks DERP TCP connections (magicsock reconnect). Live TCP sessions over DERP — with tsnet netstack, TCP connections are in the netstack; breaking the DERP conn triggers a reconnect of the underlying UDP-ish path; the TCP session should survive if it's DERP-based? Actually DERP connections are the transport for the wireguard encapsulated packets; a TCP session over DERP will stall until the DERP connection reconnects, then resume. DebugRebind: rebinds magicsock UDP sockets — existing UDP sockets get closed, which breaks the UDP transport; sessions can recover via re-STUN/disco.

The important nuance for the review: whether live netstack TCP sessions survive. In tailscale, the netstack holds the TCP state; the wireguard tunnel retransmits; when the underlying transport path is rebroken, packets are retransmitted after path recovery. So sessions typically survive but with a stall. That's what the comment claims. However, there's a real concern: the debug actions require the node to be Running; if the node is still `NeedsLogin` (e.g., user backgrounded before login, or logged out), `DebugRebind`/`DebugBreakDERPConns` on a non-running backend — rebind on magicsock that isn't connected... probably harmless but might block? With 5s ctx deadline in Go. But the Swift side: `node.debugResetConnections()` is a synchronous C call that runs on the actor — the Swift Task cancellation can't interrupt it; the Go side deadline is 5s per action — two actions = up to 10s blocked. During this, the TailscaleNode actor is blocked, so any other localAPI call (`backendStatus()`, `editPrefs`, `close()`, `loopback()`) queues. The foreground recovery task also then calls `recoverLocalAPIObserversAfterResume` which uses the SAME node actor. That's fine sequentially.

Wait, actually there's a REAL deadlock-ish hazard: `TsnetDebugResetConnections` calls `s.s.LocalClient()` — LocalClient calls `s.Start()` — fine. Then `lc.DebugAction(ctx, ...)`. `local.Client.DebugAction` POSTs to the localapi http server over the memnet pipe, handled by `localapi.Handler`. The handler for "rebind" calls `h.b.DebugRebind()` which calls magicsock rebind — this is asynchronous-ish. This runs in the Go http handler goroutine, not the calling goroutine — so no deadlock there. Fine.

But wait — a real trap: the iOS process is suspended; on resume, URLSession connections that were in flight are wedged. The recovery replaces observers. But the OLD processor's MessageReader is cancelled via `processor?.cancel()` → `reader.stop()` → `invalidateAndCancel`. Then in the retry loop, `stopStatusPolling(); processor?.cancel(); processor = nil; busErrorWatcher?.cancel()...`. Each iteration creates a new processor via startEventBus. The generation guard: `expectedGeneration == model.activeObservationGeneration` — the newConsumer's observationGeneration = generation (foregroundRecoveryGeneration). Good.

### Finding 2 (HIGH): `startStatusPolling` generation vs. `didEnterBackground` race on the "fresh response" marker. `recoverLocalAPIObserversAfterResume` waits for `model.freshLocalAPIResponseGeneration != responseMarker` within 4s. The marker is incremented by either the bus consumer notify (on generation match) or the status poll. But the status poll started with `startStatusPolling(generation: generation)` — inside, the loop guard checks `expectedGeneration == model.activeObservationGeneration` and `!didEnterBackground`. That looks OK. But note the poll task reads `self.localAPIClient` via MainActor.run — localAPIClient could be nil (set to nil in rebuild) — `if let client` guard, then skips; sleeps 5s. So if the localAPIClient is momentarily nil, the poll contributes nothing, and the 4s window expires → retry → attempt++ → 3 attempts → node rebuild. That's a 3x~4s+1s backoff ≈ 15s then a node rebuild — heavy but bounded.

Now, a real issue: **`recoverLocalAPIObserversAfterResume` sets `model.state = .Running` prematurely in the foreground task (after debugResetConnections succeeds) — "Do not keep the browser gated for ~30s". But `willEnterBackground` set `model.state = .Starting` and `socksLogProxy.setUpstreamAvailable(false)`. On foreground, if `debugResetConnections` succeeds (it returns 0 = success from both DebugActions), the code sets `model.state = .Running` and — wait, who reopens the socks gate? `startProxyAvailabilityObservation` maps `model.$state == .Running` → `setUpstreamAvailable(true)`. Since the watcher is alive (it was never cancelled on background — proxyAvailabilityWatcher is not cancelled in willEnterBackground!) — the state change to .Running publishes and reopens the gate. OK so the gate is reopened via the prefs watcher. Wait, is proxyAvailabilityWatcher cancelled? In willEnterBackground: `busErrorWatcher?.cancel()` but NOT proxyAvailabilityWatcher or prefsWatcher. Good — the gate can reopen.

BUT the state→gate path has a subtle issue: `setUpstreamAvailable(true)` releases ALL waiting clients and begins relays — even if the transport isn't actually usable yet (rebind just completed but DERP home reconnect and netmap revalidation takes seconds). The release happens on `model.state = .Running` which the foreground task sets immediately after `debugResetConnections()` returns — meaning magicsock just re-bound; DERP is reconnecting; direct paths are re-STUNning. WebKit requests will then go to tsnet's SOCKS5 which may fail with "network unreachable" (reply code 3) → -1000 error overlay! The comment claims "The synchronous repair call reached LocalBackend and both rebind actions completed, so the retained proxy is usable" — that's an overclaim: DebugRebind returns after scheduling rebind (let me check — DebugRebind calls `c.Rebind()`... does it block until rebind completes? Let me think — magicsock Rebind typically closes and recreates UDP sockets and triggers re-STUN asynchronously. DebugBreakDERPConns closes DERP conns; home DERP reconnects asynchronously. So returning from the two DebugActions does NOT mean connectivity is restored. The 2s sleep in runConnectionResetTestIfNeeded hints they know recovery takes time. So setting state=.Running immediately can open the gate while the transport is still dead → WebKit immediately tries to load → SOCKS CONNECT → tsnet dial fails (no path yet) → -1000 → error overlay. But the banner logic: `isConnected` is `model.state == .Running` — so banner clears instantly; but actual loads fail with -1000 error overlay. Hmm, but the SOCKS relay's waiting-client logic: on state bounce, new clients are "held" while !upstreamAvailable. The gate is reopened on the first .Running after bg. If that's premature, clients are released to a dead transport. The test `testExternalProcessSuspendRecoversWithoutReloadingPage` asserts the banner clears within 7s and page not reloaded — but it does NOT assert that a NEW fetch succeeds after resume, and doesn't assert the error overlay never appears... Actually it does assert nothing about nav error overlay. And `testTsnetTransportResetRecoversWithoutReloadingPage` asserts no nav-error-overlay. OK.

This is a real HIGH/CRITICAL: **premature gate open on resume** — the state is set to Running based on transport repair completing, not on actual path availability. The 4s fresh-response evidence is for observers, not connectivity. And the "banner clears within 7s" test could pass while the page is actually broken (banner gone, error overlay shown). Wait — the banner is `!tab.viewModel.isConnected` → `model.state == .Running`. Hmm.

Let me also consider: does WebKit's existing committed page survive? Yes — no reload happens because loadInitial only runs if `!didLoadInitial`. Good.

### Finding 3 (HIGH): `TSNetManager.startTailscale` — the error path `fatalError` on ANY setup error: e.g., if `setupNode()` throws or `tailscaleUp` throws. `tailscaleUp` can throw from `startEventBus` (watchIPNBus) and from `node?.loopback()`. Consider: startup while on cellular with flaky loopback... loopback can't really fail (localhost). watchIPNBus throws if URLSession setup fails — e.g., if the loopback SOCKS is not ready. Then `fatalError` — app crashes. In background-start: `startTailscaleIfNeeded` from `willEnterForeground` when `node == nil`. Hmm, actually `startTailscale` runs `try await tailscaleUp(...)`, and if the generation changed (didEnterBackground) it cancels the processor but continues to `loopback()`. Wait — actually in tailscaleUp, if `didEnterBackground` is true (startup completing while background), it skips setProcessor, cancels, but still calls loopback and publishes proxyConfiguration — but then the model is in .Starting and willEnterForeground's guard `didEnterBackground` false path... hmm wait. Let me re-read: `willEnterForeground` does `guard didEnterBackground else { return }`. If startup finished during background (didEnterBackground == true), foreground will run recovery. OK.

But `fatalError` on failure: any transient failure (e.g., the watchIPNBus request hitting a just-rebound socket, or URLSession refusing a request because the loopback listener wasn't yet up) → process crash. This is a real production crash path. On iOS, the node dir might be locked by another instance (e.g., from the pre-refactor version)? The comment mentions "two tsnet servers on the same state dir after a bg/fg cycle" — implying lockfile contention is a known failure → now fatalError instead. There's no retry around setupNode/up — the only retry is the bus restart. High severity: any start failure = hard crash with no recovery. Also `startInFlight` is only reset on the error path (before fatalError) and on... let me check: `startTailscaleIfNeeded` sets startInFlight = true; `startTailscale` sets it false ONLY in the catch path before fatalError. On success, `startInFlight` stays true forever — that's intentional (guards against double start for the lifetime). But `rebuildNodeAfterFailedResume` sets `startInFlight = false` then `startTailscaleIfNeeded()` — fine.

But WAIT — the fatalError catch: `await MainActor.run { startInFlight = false }` then `fatalError`. So the crash is deliberate on failure. The trigger: `watchIPNBus` — which creates a URLSession and starts the data task. When could that throw? `basicAuthURLRequest` → `URLSessionConfiguration.tailscaleSession(node)` → `node.loopback()` — an actor call — loopback is synchronous C, returns loopback config or throws. If the node was never started... it's started in init. Hmm, when does `loopback()` throw? If `tailscale_loopback` returns non-zero. In the C wrapper: `s.s.Loopback()` → calls `s.Start()` → start() → error if... e.g. "in-memory store is only supported for Ephemeral nodes" — not our case. Could throw if the state dir is missing? It does MkdirAll. So realistically the throws are rare. But the fatalError on a genuinely transient error (e.g., Go OOM, or a race with crashTest) is a crash. Worth HIGH (crash on any setup error, no in-app recovery).

### Finding 4 (HIGH): `MessageReader.receiveData` partial-message buffer growth & parse. `receiveData` appends data, checks if last byte is newline. BUT: if the URLSession delivers a chunk ending in `\n` followed by more in the same chunk? It appends the whole chunk; if last byte is newline, pushes whole buffer as ONE message. Then `processMessage` splits on newlines — good. But if a chunk ends with partial JSON (no trailing newline), it stays in buffer; the NEXT chunk appends. But there's a bug: if chunk 1 ends with newline (complete message) and chunk 2 is a fresh partial, that's fine. What about two messages in one chunk: appended, ends with newline → one entry containing both lines → processMessage splits. OK.

Real issue: the buffer is never bounded. If tsnet sends a huge netmap without trailing newline (e.g., partial), buffer grows unboundedly. Minor.

Bigger: `if buffer[buffer.count - 1] == kJsonNewline` — crashes if buffer is empty? `receiveData` appends data first, so buffer.count >= 1. OK.

Congestion: `if pendingMessages.count >= kMaxQueueSize { congested = true; messagesAvailableHandler?(); return }` — note it does NOT append buffer to pendingMessages and does NOT clear buffer, and the message data IS discarded (lost). Then on drain, errorHandler(queueCongested) → consumer.error → scheduleBusRestart → restart with .initialState. OK, recoverable.

But — the drain path: `drain` calls `completion(messages)` then, if wasCongested, `errorHandler?(queueCongested)`. And `messagesAvailable` after processing calls `processError` → `consumer.error` — async Task. Meanwhile, the reader was already restarted (stop() + start()) — wait, no: on queueCongested, scheduleBusRestart runs on MainActor (consumer.error sink), which cancels busRestartTask, creates a new Task → startEventBus → watchIPNBus → new MessageProcessor → `start()` → on its workQueue, `cancel()` old processor... this can race with the old processor's `drain` completion. Both processor instances' drains can run — but each processor is bound to its own reader. Hmm, `processor.cancel()` → `reader.stop()` → enqueues an operation on the reader's workQueue. Any in-flight `drain` operation already queued before stop will still execute after stop's operation? Ordering: drain was enqueued, then stop enqueued after → stop runs first (cancels task + invalidates session + clears pending), then drain runs → drains empty. Fine.

Now — the **URLSession per-request creation** in LocalAPIClient: every `backendStatus()` poll creates a NEW URLSession. URLSession(configuration:) is documented to leak if not invalidated! Each `doSimpleAPIRequest` creates `let session = URLSession(configuration: sessionConfig)` and NEVER invalidates it. URLSession objects are retained by the system (they don't dealloc) — this leaks one URLSession (and its resources) per request. Status poll every 5s = 12/min → 720/hour → unbounded memory/thread growth? Actually URLSession instances are cached by configuration... no. `URLSession(configuration:)` with a fresh configuration each time creates a brand-new session; each session holds a delegate queue, connection pools, etc. Actually — Apple docs: sessions are never deallocated; you must invalidate. The system keeps a strong reference. Hmm, but URLSession with a delegate... here no delegate is passed, so `URLSession(configuration:)` without delegate — docs say if you don't need delegate callbacks, you can use the shared session or create sessions... you still should invalidate? Actually `URLSession(configuration:)` (no delegate) sessions ARE released when no longer referenced? The docs say: "The session object keeps a strong reference to the delegate until you invalidate the session... If you do not invalidate, memory is leaked." For delegate-less sessions, the URLSession object can be deallocated... Actually there is a known leak: `URLSession(configuration:)` never deallocates regardless of delegate. Let me recall — there are many Radar reports: URLSession objects are leaked if you don't call invalidateAndCancel; they keep running background threads. Apple's docs: "If you create a URLSession object without a delegate, you can deallocate it" — no wait.

Let me be careful. The rule that's widely cited: **URLSession does not deallocate even without delegate; you must invalidate**. Hmm, actually I believe the strong-hold is: the system retains sessions with delegates. For sessions without delegates... I recall that `URLSession(configuration:)` sessions are also leaked. The commonly accepted guidance (NSURLSession memory leak) is: always `invalidateAndCancel()` when done. The URLSession+Tailscale.swift extension — let me recall what tailscaleSession(node) returns: (sessionConfig, loopbackConfig). So sessionConfig is a URLSessionConfiguration. And `doSimpleAPIRequest` creates `URLSession(configuration: sessionConfig)` per request — every status poll, every prefs edit, etc. These are never invalidated. So this is a genuine per-request leak of URLSession objects. On a 5-second poll cadence, that's ~720 URLSessions/day/hour — each holds a serial operation queue + connection cache + maybe threads. This is HIGH (unbounded resource growth on a mobile device) though each URLSession may be lightweight-ish. Actually — important detail: URLSession without a delegate and with a background or default configuration... I'll flag it as HIGH but note uncertainty, recommend invalidating or reusing a single session.

Wait — actually there's the more dangerous flip side: the MessageReader keeps its own URLSession with delegate and invalidates it properly. The poll sessions: they create one session per request. Hmm, `URLSessionConfiguration.tailscaleSession(node)` — let me think about what it does: it probably sets the SOCKS proxy config pointing at the loopback. I didn't read URLSession+Tailscale.swift. Let me remember its content — 41 lines. It creates a session config with connectionProxyDictionary pointing at the tsnet SOCKS proxy, and calls node.loopback() to get the address. And a timeoutInterval maybe. I should have read it but I didn't. Based on the name, yes.

### Finding 5 (HIGH): SocksLogProxy `stop()` doesn't close established relays. In `rebuildNodeAfterFailedResume`: `socksLogProxy?.stop()` — but established relay connections (pumps) are NOT tracked/cancelled! Only the listener and waitingClients. When the tsnet node is rebuilt, its loopback proxy gets NEW credentials (proxyCred is random per server!). The old relays are still pumping bytes to the OLD upstream address 127.0.0.1:oldport — which is now dead (old node closed its loopback listener). So established connections will hang/fail after the rebuild — WebKit sees socket errors. But those connections belong to a page that was mid-connection during the fallback rebuild — the page doesn't reload, so it will show the error overlay or hang. The relay's pump: after node close, the old upstream connection will error → pump cancels both. OK eventually resolves, but no log. Also: the socksLogProxy instance is replaced with nil; when the new node starts, `proxyConfig` creates a NEW relay with the new upstream port. Fine.

But there's a subtle bug in `SocksLogProxy.handle`: `client.stateUpdateHandler` — for waiting clients, it's set. But when the client is released via `setUpstreamAvailable(true)`, `beginRelay` sets `client.stateUpdateHandler = nil`. But if the waiting client was already cancelled (e.g., iOS killed the socket), it stays in waitingClients until a `state == .cancelled` callback arrives (which may never fire if stateUpdateHandler was replaced?) — actually the handler is set in handle(); if cancelled, handler fires .cancelled and removes. OK. But there's a leak: waiting clients never time out! If the gate never reopens (node stuck in Starting), waiting clients accumulate forever — memory + fd leak. Each waiting client holds an NWConnection with TCP buffers. And NWConnection has no timeout here. If a page load is held and the user navigates away... the old connection from WebKit may be cancelled by iOS → .cancelled fires → removed. Probably OK but unbounded in pathological cases (e.g., 10 waiting clients from a single hung navigation). LOW/MEDIUM.

Also — the waiting-client "hold" mechanism: clients connect to the relay, send the SOCKS greeting, and the relay never reads them → TCP backpressure → WebKit's request stalls. That's the design. But — the greeting bytes sit in the kernel buffer; when released, beginRelay pumps them. OK.

One more SOCKS relay correctness issue: **`pump` recursion and half-close**. `pump` cancels BOTH connections on isComplete/error of EITHER direction. In SOCKS5 over TCP, after the CONNECT reply, the relay should support half-close (FIN from client shouldn't kill the upstream until EOF in the other direction). Here: client sends FIN (isComplete on receive), pump cancels BOTH client and upstream → upstream connection closed → tsnet sees connection close → server-side (WebKit page) sees EOF. For most web traffic this is fine (HTTP/1.1 keep-alive — client half-close is unusual). But for protocols like WebSocket/SSE (streaming uploads), half-close matters. E.g., an HTTP request with `Expect: 100-continue`... or a fetch with a body stream. A page doing a long-lived upload could break. This is a genuine correctness gap for a pass-through proxy: it should propagate half-close, not cancel both. MEDIUM.

Also `pump`'s `.contentProcessed { _ in ... }` — the completion fires when the send buffer accepts the data (not when the remote reads it) — fine, this is just flow control. But there's no buffering between receive and send: if `to.send`'s completion is delayed, the recursion stops — the `from.receive` is NOT re-issued until the send completes → backpressure propagates. Fine.

BUT there's a recursion-safety concern: `pump` recursion is asynchronous (completion handlers), so no stack growth. OK.

Wait — the `.contentProcessed` completion captures `self` weakly and recurses — but the connection objects are captured strongly by the closure chain. If the relay is stopped, the listener is cancelled but these closures keep pumping. The old upstream dies, error propagates, both cancel. OK.

Another: `beginRelay` is called from `setUpstreamAvailable` on the queue — client.start(queue: queue) — but handle() is invoked on the listener's queue (also `queue`). `client.stateUpdateHandler = nil` then `beginRelay` sets a NEW stateUpdateHandler? No — beginRelay sets `client.stateUpdateHandler = nil` FIRST, then creates upstream with `upstream.start(queue: queue)` and pumps. But it never sets an error/state handler on the upstream NWConnection! If the upstream connection fails to connect (e.g., tsnet loopback proxy port closed because node not actually listening — e.g., gate opened prematurely), the upstream `.failed` state fires... who observes? `pump(from: upstream, ...)` calls `upstream.receive(...)` — if the connection failed, receive returns an error → pump cancels both → OK. So a failed upstream yields `socks[ID] FAILED ...`? No — the server reply never arrives because the connection failed before the SOCKS handshake. The client side also never gets a reply → WebKit waits... until what? The client connection gets cancelled by pump when upstream errors. So WebKit's request ends with a socket close (no SOCKS reply) → -1000 or -1005. The log would show nothing for that attempt (no CONNECT parse since the request was never forwarded). Hmm — actually the client's SOCKS greeting was already parsed? Only if data flowed. If upstream never connected, client bytes are parsed (pump from client → inspectClientBytes logs the CONNECT attempt "request reached the tailnet proxy") but no reply → no outcome logged. Slightly misleading log ("reached the tailnet proxy" when the upstream connection failed). MEDIUM logging correctness.

### Finding 6 (HIGH): `TSNetManager.willEnterForeground` — `guard let node else { startTailscaleIfNeeded(); return }`. But `didEnterBackground` is set false FIRST, then this path. If node == nil (never started or was rebuilt), startTailscaleIfNeeded → new start with startInFlight guard. OK. But if the node exists and foreground recovery runs — `foregroundRecoveryTask = Task { ... }` — a Task on MainActor (since willEnterForeground is MainActor, the Task inherits MainActor). Inside: `try await node.debugResetConnections()` — the actor hop to TailscaleNode. But — the SYNCHRONOUS C call runs on the actor's executor thread. While it runs (up to 10s for two DebugActions), the actor is blocked... but each has its own 5s ctx deadline. Actually wait — the C wrapper: `tailscale_debug_reset_connections` → `TsnetDebugResetConnections` — `lc.DebugAction(ctx, "rebind")` — the ctx is 5s. `DebugAction` → localapi HTTP POST over memnet pipe → handled by Go http server goroutine → `h.b.DebugRebind()`. If the backend is in a bad state (e.g., still initializing after suspension), could `DebugRebind` block longer than 5s? The ctx deadline applies to the HTTP request round-trip; the handler runs regardless. If `h.b.MagicConn()` is nil (backend not started) → panic in handler → Go panic inside http server → recovery → connection error → returns within 5s. If backend started, Rebind is quick. OK so worst ~10s actor block. During that time, any other `await node.xxx()` from other code (e.g., `setExitNodeEnabled`'s editPrefs Task → LocalAPIClient → `node.loopback()` via tailscaleSession) queues. The status poll Task calls `client.backendStatus()` → `basicAuthURLRequest` → `node.loopback()` — blocked. These all queue on the actor — they'll eventually run. No deadlock, just stall. But `backendStatus()` requests from the OLD poll (pre-background) were wedged URLSessions — they're replaced.

Deadlock check — `local.Client.DebugAction` with `ctx` 5s and the handler doing `h.b.DebugBreakDERPConns()`: does that block holding a lock that the SOCKS5 dial path needs? DebugBreakDERPConns iterates derp http clients and closes them — connSet map with lock. The SOCKS dial path (socks5 handler) calls `s.s.Dial` → netstack → wireguard → magicsock — magicsock lock? Closing DERP conns while dialing might briefly contend but not deadlock. OK.

### Finding 7 (HIGH/MEDIUM): **The `-1000` premature gate-open (already covered) and the "reconnect without reload" claim vs. `loadInitial` + `applyProxy`.**

`applyProxy(proxy)` — called whenever `model.$proxyConfiguration` publishes (every refreshProxyPolicyIfNeeded republish). It sets `dataStore.proxyConfigurations = [proxy]` EVERY time — even if unchanged! Does setting `proxyConfigurations` to the same value reset WebKit's connection pool? Setting `WKWebsiteDataStore.proxyConfigurations` triggers re-evaluation of existing connections — WebKit will tear down existing proxied connections when the proxy config changes (even same value?). The publish only happens when policy changed (refreshProxyPolicyIfNeeded guards with `guard policy != model.proxyPolicy`). And `proxyConfig` is published once at start and once per policy update. But ALSO `applyProxy` does `if !didLoadInitial { loadInitial() }`. And `applyConnectionState` — on reconnect, does nothing if already loaded. OK.

But here's the real WebKit teardown question: does WebKit's connection pool survive the proxy CONFIG staying constant while the actual SOCKS listener changes (relay restart after node rebuild)? After node rebuild, the loopback port CHANGES (new random port + new creds) → new relay → new proxyConfig → published → applyProxy sets new config → WebKit tears down connection pool → in-flight requests fail; committed page stays. So during a node-rebuild fallback, the current page's live connections die silently (no reload, but subsequent XHR/fetches fail with -1000 until the page issues new requests). The UI test for the fallback (`-UITestLifecycleRecoveryRace` asserts "complete" not "fallback-node-rebuild") — the fallback path isn't covered by tests. MEDIUM/HIGH.

### Finding 8 (MEDIUM): `MessageProcessor.start` — `stateLock.withLock { cancelled = false ... }` is on the processor's workQueue; `messagesAvailable` may fire from the reader's queue between... it's OK.

But `MessageProcessor.deinit` calls `cancel()` (synchronous — `reader.stop()` enqueues an op) — deinit on a class with an OperationQueue; fine.

Actually there's a subtle one in MessageProcessor: `start` is called with `errorHandler` defaulting to a closure that calls `self.processError(error)` → `consumer.error` — the TSNetConsumer's error publishes `self.error = error` on MainActor → TSNetManager's `consumer.$error.sink` → scheduleBusRestart. The busObserver in startEventBus is created via `consumer.$error.sink` — `@MainActor @Published var error` — the sink receives on main. Fine.

**Race**: `startEventBus` returns `processor` but the busObserver sink may fire BEFORE `busErrorWatcher` is installed (the sink is created before `await MainActor.run {...}` installs it). Since `$error` is a @Published CurrentValueSubject-like, `.sink` delivers the CURRENT value immediately on subscribe! `consumer.$error` — the initial value is nil, so the sink fires with nil → `guard let self, let error else return` → returns. OK — but on the SECOND call to startEventBus, `consumer.error` could be non-nil from a previous error (set during recovery), and the sink fires immediately → `scheduleBusRestart` with a stale error → spurious restart. Wait — in scheduleBusRestart, `consumer.error = nil` is set after. In recoverLocalAPIObserversAfterResume, a newConsumer is created per attempt, so its error starts nil. In scheduleBusRestart's loop, startEventBus is called again with the SAME consumer whose error was set to nil at loop start. But `consumer.$error.sink` fires immediately with nil — fine. OK, mostly safe.

BUT the immediate-value delivery: `consumer.$error.sink` — TSNetConsumer.error is `@MainActor @Published var error: Error?`. The sink closure captures `[weak self]` and `self.scheduleBusRestart(...)`. Since @Published initial value is nil and sink fires on subscribe with the current value → nil → guarded. OK.

### Finding 9 (MEDIUM): **TailscaleNode actor + deinit double-close during rebuild.** In `rebuildNodeAfterFailedResume`: `try await node.close()` — actor close: `tailscale_close` deletes from servers map, closes. Then `self.node = nil` → deinit → `tailscale_close` again → getServer nil → EBADF → no-op. Fine. But — TailscaleNode.deinit runs on whatever thread releases the last reference; it calls `tailscale_close` synchronously — with the actor's serial executor possibly mid-call? No — deinit runs when refcount hits zero; any in-flight actor calls hold references... The node.close() call completed before nil. Any queued-but-not-yet-run actor methods (e.g., a pending `loopback()` from a status poll) hold a strong ref to the actor until they run → deinit can't run until those finish. But those queued methods run AFTER close() and operate on a closed server: `loopback()` on a closed tsnet → `s.s.Loopback()` → `s.Start()` → doInit already ran → returns initErr nil → loopbackListener was closed → net.Listen again? Wait: `Loopback()` checks `if s.loopbackListener == nil` — after close(), `s.loopbackListener` is still non-nil (close() doesn't nil it!) — so `Loopback()` returns the OLD closed listener's address → URLSession config with dead port → requests fail. But generation checks on the model side discard results. The status poll task was cancelled. So mostly benign.

BUT here's the thing: after rebuild, a NEW node is created with the SAME state dir. The old node's `close()` ran `lb.Shutdown()`. The lock on the state dir: tsnet doesn't lock the dir exclusively at the fs level? There's `flock` on the state file? tsnet uses a `lazyStore` with file lock (`tailscale.com/types/persist`? Actually the state store locks via `fileLock` in `ipnlocal`?) — the known symptom "two tsnet servers on the same state dir" implies a lock. tsnet Server has `s.Store` with a `flock` on `ipnstate.json`? I believe tsnet uses `store.New(lazyStore)` with file locking — creating a second server on the same dir fails with "could not acquire lock". The rebuild path: old node close() completes (releasing the lock) BEFORE `startTailscaleIfNe

--- ANALYSIS CHUNK ---

Let me synthesize everything I've read into the requested review. Let me carefully trace the key operation sequences to identify real bugs.

Let me review the key facts I've gathered:

## Architecture summary
- `TSNetManager` (MainActor) owns one `TailscaleNode` per workspace, plus `LocalAPIClient`, `MessageProcessor`, `SocksLogProxy`.
- `TailscaleNode` is an actor wrapping C calls (`tailscale_new`, `tailscale_start`, `tailscale_loopback`, `tailscale_close`, `tailscale_debug_reset_connections`).
- `tailscale_start` → `tsnet.Server.Start()` → `doInit` → `start()` → creates LocalBackend, sets `WantRunning=true`, `lb.Start(ipn.Options{UpdatePrefs, AuthKey})`, and if state is NeedsLogin runs `StartLoginInteractive`. This matches the comment about not needing `Up()`.
- `TailscaleNode.loopback()` → `tailscale_loopback` → `s.s.Loopback()` → starts loopback listener with SOCKS5 + localapi HTTP.
- LocalAPI client uses URLSession through the loopback SOCKS proxy with basic auth `tsnet:<localAPIKey>` and `Sec-Tailscale: localapi` header.
- MessageReader: URLSessionDataDelegate on serial OperationQueue, newline-delimited JSON, max queue 24, congestion → queueCongested error → consumer error → bus restart.
- MessageProcessor: drains via reader.drain, decodes, forwards to consumer (actor) via Task.
- TSNetConsumer: actor with generation check; forwards to MainActor model.
- Status poll task: every 5s `backendStatus()`.
- Foreground recovery: `debugResetConnections` (rebind + break-derp) then `recoverLocalAPIObserversAfterResume` (rebuild observer with fresh-response evidence), with 3-attempt limit → `rebuildNodeAfterFailedResume` → node.close() + new node.
- Background: keeps node, cancels observers, sets state = .Starting, closes SOCKS gate.
- SocksLogProxy: NWListener loopback, per-connection relay with waiting-client queue, gate.

## Now let me trace specific bugs

### Bug candidate 1: `startTailscaleIfNeeded` guard reset on failure path
`startTailscale()` catch sets `startInFlight = false` then `fatalError` — process dies, so irrelevant. But `rebuildNodeAfterFailedResume` sets `startInFlight = false` before calling `startTailscaleIfNeeded()`. OK.

But wait: in `willEnterForeground`, if `node == nil`, calls `startTailscaleIfNeeded()`. `startInFlight` may still be true if startup is in flight... but startup in flight means node will exist soon. Fine.

### Bug candidate 2: `tailscaleUp` generation check and `setProcessor` — subtle race where `setLocalAPIClient` and `startStatusPolling` were already called during startup, then `didEnterBackground` becomes true mid-startup, then `willEnterForeground` runs recovery. In `tailscaleUp`, if `didEnterBackground` is true at that point, it cancels the processor but still publishes proxy config. Then foreground recovery restarts observers. OK by design.

But: `setLocalAPIClient` runs `startPrefsObservation()` and `startProxyAvailabilityObservation()` — these are never cancelled on background (they're Combine sinks on model.$prefs and model.$state, not URLSession tasks; they survive, which is fine, they just call MainActor methods guarded by nil checks).

### Bug candidate 3: SocksLogProxy `pump` recursion — `to.send` with `.contentProcessed { self?.pump(...) }`. If `to` is cancelled or send fails, the completion still fires with error, and pump re-arms `from.receive` — but `from` was cancelled → receive completion fires with error → cancels both again. Not a leak per se. But there is a real issue: when one direction ends (half-close: `isComplete`), pump calls `from.cancel(); to.cancel()` — cancel BOTH connections. For a SOCKS relay, half-close semantics: when client finishes sending (EOF), the upstream should half-close write, not kill the whole connection. NWConnection has no half-close API (no shutdown write). So canceling both on half-close kills an established relay when, e.g., an HTTP client sends request and half-closes write (rare for SOCKS, but happens with some protocols). Actually for SOCKS5 CONNECT, after the handshake, WebKit treats the connection as a full duplex TCP stream. Some apps do shutdown(WR) — WebKit/CFNetwork rarely. This is a MEDIUM at most.

More important: **waiting client leak on listener stop**: `stop()` cancels waiting clients. OK. But `setUpstreamAvailable(false)` doesn't touch established relays — by design.

**Waiting-client timeout**: waiting clients are held with no timeout. If the tailnet never recovers (e.g., stays NeedsLogin), WebKit's request hangs until its own timeout. iOS may keep the socket; that's the intended "hold" behavior. But on a status poll that never comes back (bus dead AND poll dead), waiting clients accumulate — bounded by WebKit's connection pool (usually 6 concurrent), so bounded leak. LOW/MEDIUM.

**BUG: `handle()` reading `client.stateUpdateHandler` while `beginRelay` sets it to nil** — a race between `handle` (sets stateUpdateHandler to track waiting) and `setUpstreamAvailable(true)` (calls beginRelay which sets `client.stateUpdateHandler = nil`). Both run on the same serial `queue`, so no race. OK.

**BUG: waiting client canceled by WebKit while gate closed** — the stateUpdateHandler removes from waitingClients on cancel. But then `setUpstreamAvailable(true)` later iterates `waitingClients` and relays them — a cancelled connection in the dict would be relayed. But the stateUpdateHandler removes it on .cancelled, and it's on the same serial queue, so ordering is safe. OK.

### Bug candidate 4: `debugResetConnections` semantics — does rebind+break-derp actually repair iOS suspension?

The critical question: does iOS suspension actually damage magicsock UDP sockets? On iOS, when an app is suspended, its sockets are closed by the kernel (for UDP, the socket may survive but the network path changes). The claim in the code is that rebind+break-derp repairs this. But there's a subtlety: **the tsnet SOCKS server / loopback listener are TCP loopback sockets on 127.0.0.1** — those survive suspension. The magicsock UDP socket: iOS kills network sockets of suspended apps (this is well documented — cf. Apple docs about socket invalidation during suspension). Actually on iOS, when an app is suspended, the system may close sockets. On resume, the app must recreate them. `DebugRebind` closes and re-creates the UDP socket. And DERP conns break forces reconnection. This is a reasonable repair. But: does the LocalBackend continue to run while suspended? Go goroutines are frozen (SIGSTOP-equivalent). On resume, all Go code resumes. The stale UDP socket might receive ICMP errors or be dead; rebind fixes.

**Real issue**: `TsnetDebugResetConnections` runs synchronously from the Swift side through the C bridge — it calls `lc.DebugAction(ctx, "rebind")` and `"break-derp-conns"`. `DebugAction` is an HTTP POST to the in-process localapi server (memnet). That's fine — but `h.b.DebugRebind()`... let me check what DebugRebind does. It rebinds magicsock. And `DebugBreakDERPConns` closes DERP conns. These are synchronous calls on the localapi server goroutine. The 5s deadline is on the client side (`ctx, cancel := context.WithTimeout(...)`), but the underlying localapi handler runs on its own goroutine and is not cancelled by the client's context — the handler's `h.b.DebugRebind()` runs to completion regardless. So the 5s timeout prevents the *caller* from blocking forever, but the work continues. If DebugRebind blocks (network calls?), the handler goroutine stays blocked — subsequent localapi calls (status polls!) would queue behind it on the same HTTP server. Actually the http.Server handles each request on its own goroutine, so a blocked handler doesn't block other requests... except `Serve` uses a semaphore (GOMAXPROCS) — fine.

Let me check DebugRebind for blocking:

Let me not make more tool calls. From memory of the tailscale codebase: `DebugRebind` calls `b.magicsock.Rebind()` which closes and reopens UDP sockets — it does DNS? No, it just rebinds. Should be fast. OK.

**Bigger issue: `TsnetDebugResetConnections` uses `s.s.LocalClient()`** which calls `s.s.Start()` — if the server was never started, Start would run doInit. But it's called only when node exists and Running. Fine.

### Bug candidate 5: `willEnterForeground` calls `debugResetConnections` **unconditionally and only when `didEnterBackground`**. But iOS scene phases: `.active` fires at launch (guarded by didEnterBackground). What about `.inactive` → `.active` transitions without `.background`? Guarded — good. What about rapid background/foreground? `foregroundRecoveryGeneration &+= 1` per background. In willEnterForeground, `let generation = foregroundRecoveryGeneration` — captured AFTER background incremented it. The recovery Task checks `generation == self.foregroundRecoveryGeneration`. If user re-backgrounds mid-recovery, `willEnterBackground` bumps generation → recovery task's checks fail → task exits at next guard. Good. But note the recovery Task's `guard` checks happen at: start, after debugResetConnections, before recoverLocalAPIObserversAfterResume... the `while` loop inside also checks generation. OK.

**But wait**: in `willEnterForeground`, the transport repair `debugResetConnections` happens BEFORE checking generation again in the guard inside the task: the task body's first guard `generation == self.foregroundRecoveryGeneration` — if background happened between willEnterForeground and the task starting, the guard fails. But there's a race: `foregroundRecoveryTask` is set to the new Task after cancellation of the old. `willEnterForeground` itself: `foregroundRecoveryTask?.cancel()` then assigns new. Two rapid foregrounds: second willEnterForeground cancels first task and starts new. First task might be inside `try await node.debugResetConnections()` (synchronous C call) — cancellation can't interrupt it, but the C call itself is bounded by Go-side 5s deadline... actually the C call runs on the TailscaleNode actor. Both recovery tasks await the same actor — serialized. Fine.

### Bug candidate 6: **`debugResetConnections` on the MainActor-isolated TSNetManager is called via `try await node.debugResetConnections()` — that's a synchronous throw (not async) on an actor!** Look: `TailscaleNode.debugResetConnections()` is `public func repairConnectionsAfterResume() throws` — NOT async. Calling `node.debugResetConnections()` from async context — actor-isolated sync method — the await hops to the actor and blocks the actor's executor until the C call returns. Since it's sync, it blocks the actor's thread for up to 5s (Go deadline). During that time, other actor calls (backendStatus polls via LocalAPIClient's URLSession — those don't go through the actor except `basicAuthURLRequest` → `URLSessionConfiguration.tailscaleSession(node)` which awaits `node.loopback()` — cached, so returns fast... let me check URLSession+Tailscale) queue behind. Actually wait — the localAPI client calls `self.node` only in `basicAuthURLRequest` (via `URLSessionConfiguration.tailscaleSession(node)` which calls `node.loopback()`). `loopback()` caches `loopbackConfig`, but it's `private var loopbackConfig` on the actor, so the call still needs actor access — it's cached after first call, so returns quickly, but still serialized on the actor executor.

So during the ~0-5s synchronous debugResetConnections, the actor executor is blocked. Status polls (`backendStatus`) that try `basicAuthURLRequest` will queue behind it. But since it's a synchronous C call inside an actor method invoked with await, the actor's executor thread is busy — all other actor calls (even cached loopback) block until it returns. The Go-side deadline is 5s max, so worst case 5s stall of the actor, during which localAPI calls queue. URLSession has its own timeouts (60s default). A 5s stall is tolerable. But — the C call itself: `TsnetDebugResetConnections` → the localapi DebugAction POST over memnet. The localapi HTTP handler runs on its own goroutine; the POST is served in-process. The client ctx timeout is 5s; if the handler takes longer, the client returns error but the handler keeps going. Then subsequent DebugAction from a *second* foreground would queue. Minor.

**Actually a real concern**: `DebugRebind` — let me recall — in newer tailscale it calls `b.magicsock.Rebind()` and also `b.MagicConn().SetDERPRegionID` etc. There's an important known issue: `DebugRebind` in ipnlocal:

```go
func (b *LocalBackend) DebugRebind() error {
	if b.magicsock == nil {
		return errors.New("magicsock not started")
	}
	return b.magicsock.Rebind()
}
```

That's fine.

### Bug candidate 7: **The `-1000` / WebKit proxy config and retained old proxy after node rebuild**

When `rebuildNodeAfterFailedResume` replaces the node, the new node's loopback gets a NEW port and NEW credentials (random). `model.proxyConfiguration` is republished (via the new `startTailscale` → `tailscaleUp` → `loopback()`). BUT the new `proxyConfig` creates a new `SocksLogProxy` only if `socksLogProxy == nil` — old one was stopped and nil'd in rebuild. Good.

**But here's the WebKit problem**: `BrowserViewModel.applyProxy` sets `dataStore.proxyConfigurations = [proxy]`. If the node was rebuilt while WebKit had an open connection pool to the OLD proxy address, does setting `proxyConfigurations` tear down existing connections and re-point them? The docs claim WebKit keeps existing sessions. But if the loopback port CHANGED (new node), any connections still in the pool to the old port are dead — WebKit will eventually get connection errors (-1000). If the port stays the same... it won't, new node = new random port. This is the "node rebuilt but WebKit retains an old proxy config" consequence the prompt asks about. Actually the code republishes proxyConfiguration, so WebKit gets the new address on the next connection. Existing established connections die — pages mid-load fail with -1000. This is inherent.

### Bug candidate 8: **TabManager unload on workspace switch** — `WorkspaceRoot.onDisappear { tabManager.unloadAllWebViews() }`. Then when the workspace becomes active again, `BrowserRootContent` shows `currentTab` → `BrowserView(model: tab.viewModel)` → `RawWebView` → `model.makeWebView()`... let me check RawWebView:

I didn't read RawWebView.swift (23 lines). `BrowserViewModel.makeWebView()` — if `pendingLoadURL` set → loadResolved; else `loadInitial()` → `guard !didLoadInitial, isConnected` — after unload, `didLoadInitial = false`, so loadInitial proceeds if isConnected. But `unloadWebView` sets `pendingLoadURL = url` if url != nil. On next makeWebView, pendingLoadURL consumed. Good — tabs restored to their last committed URL, not home. So tabs going home would require `viewModel.url == nil` at unload AND `isConnected` false at recreate... If unloaded while NOT connected (e.g. during background), url exists, pendingLoadURL set. When recreated: `loadResolved(pendingLoadURL)` — no isConnected guard — good.

**But** there's a subtle bug: `makeWebView()` is called when the tab is selected. `loadInitial()` is guarded by `isConnected`. If a tab is created (openChatTab) while not connected, `loadInitial` holds; when connected via `applyConnectionState` or `applyProxy`, `loadInitial()` runs. `applyProxy` is only called when `proxyConfiguration` CHANGES (published). OK.

**Critical path for "tabs sent home"**: `openChatTab` loads homePage.url. `TabManager.init` — if persisted session exists, restore; else openChatTab (home). The persisted session stores `displayURL`. On relaunch, tabs restored from `StoredBrowserTab(url:)`. If `displayURL` was the home URL because the tab never loaded (never connected), restored tabs show home. That's expected.

**Where could a loaded page get reset to home?** Candidates:
1. `tab.unloadWebView()` while `viewModel.url == nil` (page never committed) AND `viewModel.pendingLoadURL` nil → next makeWebView → `loadInitial()` → home. When is url nil with didLoadInitial true? If load was in progress (provisional) and unload happens: `webView.stopLoading()`, `if let url { pendingLoadURL = url }` — url is the committed URL; if none committed, pendingLoadURL = nil → next loadInitial → home. So switching away mid-provisional-load resets that tab to home. Minor.

2. **Workspace switch destroys WorkspaceRoot subtree** — `.id(ws.id)` — switching workspaces tears down `WorkspaceRoot` for the old workspace and creates a new one. `onDisappear` fires → `unloadAllWebViews()`. When switching back, a NEW `WorkspaceRoot` is created; `tabManager` is the SAME object (workspace-owned lazy). `hasConnected` @State — reset to false! So on switching back, `hasConnected` starts false → shows ConnectionGateView until statusViewModel.running flips. If node still running, `.onAppear { if statusViewModel.running { hasConnected = true } }` → quickly shows browser. But if the tailnet dropped while away (background), the user sees the gate again... The gate shows "connect" flow; `hasConnected` only flips when Running. During reconnecting, gate shows spinner. Then browser restored from tabManager — tab recreated via makeWebView → loads pendingLoadURL. So tabs don't go home here unless url was nil.

3. **`-UITestResetHomePage` / relaunch** — only tests.

4. **`BrowserViewModel` recreated on workspace switch?** No — tabManager is workspace-owned lazy, persists across view subtree teardown.

5. **SwiftData modelContainer reapplication** — the comment explains it's applied at TabbedBrowserView level to avoid rebuilding.

6. **Process termination / relaunch**: tabs persisted via WorkspaceStore.saveTabs (SwiftData? Let me recall — `WorkspaceStore.loadTabs/saveTabs`). Restored on relaunch. If the app is killed while the page was mid-load with no committed URL, restored tab shows whatever displayURL was last persisted — the onMetadataChange fires only on title/URL change, so displayURL = last committed. Restored tab's viewModel gets initialURL = restored URL, `loadInitial()` fires when connected → reloads that URL. Not home.

Hmm — but wait, there IS a serious "tabs sent home" candidate: **`TabManager.unloadHiddenTabs()` in `openChatTab`** — no. **`select()`** — no.

Let me think about **the `ReconnectingBanner` + `loadInitial` interaction**: `loadInitial` guards `isConnected`. After background, model.state = .Starting → isConnected false. If the selected tab's webView was NOT unloaded (only hidden tabs are unloaded; the selected tab keeps its webView across background!), then on foreground, transport repaired, state = .Running → `applyConnectionState(true)`: `if !didLoadInitial { loadInitial() }` — didLoadInitial true, so NO reload. Good — page kept.

**But the selected tab's WKWebView survives background?** The app is suspended; on resume, the WKWebView and its page process (WebContent) may have been terminated by the system. Then `webViewWebContentProcessDidTerminate` fires → `navigationError(URLError(.cannotLoadFromNetwork))` → navError overlay with Retry — the page shows "Unable to load" until user taps Retry. It does NOT auto-reload. So on resume after process-kill, user sees the error overlay, not the page. Plausible UX complaint but not "tabs go home".

Hmm, actually there's a subtle one: after WebContent process termination, `webView.url` returns nil (the webview's committed URL is lost!). Then `webViewWebContentProcessDidTerminate` → `navigationError(..., for: webView.url ?? initialURL)` — initialURL here is the tab's initialURL = home! So the error overlay shows home URL. And `url` KVO — `.url` becomes nil → `acceptSameDocumentURL` guard fails (current = nil) → url stays stale (previous value). `refreshState` only updates url on commit/finish. So the address bar keeps the old URL. But the page is blank with error overlay. If the user hits Retry, `reload()` → `navError?.url` = home URL (since webView.url was nil → initialURL passed) → loads home! Wait — `navigationError(_ error:for url:)` receives `url = webView.url ?? initialURL` — if webView.url is nil, url = initialURL = home. Then `failedInitialURL = url == initialURL ? url : nil` — sets failedInitialURL to home. Retry → `load(url: failedURL)` → loads HOME. **So a WebContent termination during background/resume + Retry = tab sent home.** Also the error overlay's URL shows home. This is a real "tabs sent home" path (plausible reports). Also, `webView.url` during a provisional failure can be the OLD committed URL or nil. The code says "webView.url can be the provisional destination or the old committed page" — and uses `NSURLErrorFailingURLErrorKey` first. For process termination there's no failing URL key, so it falls to `webView.url ?? initialURL`.

Also, **a crash of the WebContent process with no committed URL yet** — same.

There's another candidate: **`acceptSameDocumentURL` only accepts same-origin changes; after process termination, KVO `url` becomes nil then maybe restored?** Not relevant.

OK, and the biggest one: **`unloadWebView` on the SELECTED tab never happens on background** (only hidden tabs). The app keeps the selected webview alive across suspension. If WebKit's web content process is killed during suspension (common on iOS under memory pressure), resume shows the error overlay; and the KVO `\.url` may deliver nil. Then the next `loadInitial()`... no.

Wait, actually there's a more insidious one. `makeWebView()` → `if let pendingLoadURL { loadResolved } else { loadInitial() }`. `unloadWebView()` sets `pendingLoadURL = url` — the *validated* committed URL — good. But it also sets `didLoadInitial = false` and **does not preserve `navError`**. Fine.

But hold on — in `unloadWebView`, `if let url { pendingLoadURL = url }` — `url` is `viewModel.url`, the validated address-bar URL. It's `nil` until first commit. So a tab that was loading its first page (never committed) gets unloaded → pendingLoadURL nil → next select: loadInitial → home or initialURL. **For the FIRST tab this is initialURL = home page URL.** So: open app, tap "+" (new tab), type URL, press Go, immediately switch tabs — the new tab unloads (it's hidden now), pendingLoadURL nil (not committed), switch back → loads home. Plausible "tabs sent home".

Also, `loadInitial()` for a restored tab with initialURL = restored URL — not home. Fine.

### Bug candidate 9: MessageReader queue congestion + `kJsonNewline` buffer check

`receiveData`: `if buffer[buffer.count - 1] == kJsonNewline` — if `data` is empty, `buffer[buffer.count-1]` could index out of bounds! If `data.isEmpty` → `buffer` unchanged → `buffer[buffer.count-1]` — if buffer is empty, crash. When can `didReceive data` deliver empty data? URLSessionDataDelegate can deliver zero-length chunks (rare, but possible with chunked responses or empty trailers). If `buffer` is empty and `data` is empty → `buffer[buffer.count - 1]` = `buffer[-1]` → **crash (fatalError/exception) in a URLSession delegate on the serial queue**. Actually `buffer[buffer.count-1]` with count == 0 → index -1 → out of bounds trap → crash. This is a real CRASH bug, albeit rarely triggered. Let me double check: `data` param of `didReceive` — could be zero-length? The docs say data tasks deliver data; zero-length deliveries are possible (e.g., for connection keepalives?). Hmm, WebKit/CFNetwork may deliver empty chunks on some responses. It's a latent crash. MEDIUM (hard to trigger deterministically).

Also the `congested` check: `if congested { return }` in `didReceive` — but `receiveData` is also called... only from didReceive. OK.

Wait, also: `receiveData` adds a workQueue operation per chunk — with `buffer.append(data)` then checking last byte == newline. If a message is split across multiple chunks without a trailing newline, fine. But if a chunk contains MULTIPLE newline-delimited messages: `pendingMessages.append(buffer)` appends the whole buffer (multiple lines in one Data), then processor splits by newline. OK.

**Congestion path**: `pendingMessages.count >= kMaxQueueSize` → `congested = true; messagesAvailableHandler?(); return` — note the buffer still holds the current message and is NOT cleared; next chunk appends more. `drain` resets `congested = false`, and if wasCongested → errorHandler(queueCongested) → consumer.error → bus restart. But the buffered (non-newline-terminated) partial message remains in `buffer`; on restart, `start()` resets buffer. OK.

**Ordering issue in drain**: `drain` runs as an OperationQueue op; `receiveData` ops are queued before/after. Since the queue is serial, drain's op is ordered after all previously queued receives. But `didReceive` (delegate) is called on the same serial queue (delegateQueue: workQueue), so ordering is guaranteed. But `messagesAvailableHandler` is called synchronously from within the receive op; it triggers MessageProcessor's `messagesAvailable()` which calls `reader.drain(...)` — which enqueues ANOTHER op on the same queue. Since we're currently executing an op on that queue, drain's op runs after the current op completes. Fine — no deadlock.

**MessageProcessor messagesAvailable/redrain loop**: `reader.drain` completion runs `processMessage` for each message which enqueues ops on `workQueue` (the processor's own queue — separate from reader's). Wait — `processMessage` adds to `self.workQueue` (MessageProcessor's). And `messagesAvailable` is invoked from the reader's queue via the handler. `drain`'s completion block runs on reader's workQueue (it was enqueued there). Inside, `processMessage` enqueues on the processor's queue. The redrain logic: `if redrain { messagesAvailable() }` — called from reader's queue, re-enters the stateLock, re-drains. OK, no deadlock, though it could spin if messages keep arriving: each drain consumes; redrain only when drainRequested. Bounded.

**Potential issue**: `MessageProcessor.start` calls `cancel()` first (cancelled=true) then resets flags and `reader.start`. But `cancel()` sets `cancelled = true` and `reader.stop()` enqueues a stop op. Then the flags are reset to false and `reader.start` enqueues a start op. Serial ordering on reader's queue: stop then start. OK.

But `processMessage` still enqueues ops after cancel — the queue drains them; they call `consumer.notify` — generation check discards. OK.

### Bug candidate 10: **`TSNetConsumer.error` published from non-main contexts** — `@MainActor @Published var error` — the actor sets it via Task @MainActor. The `consumer.$error` sink is in `startEventBus` — `await consumer.$error.sink { ... }` — this is a Combine sink on a @Published inside an actor; `.sink` closure runs on whatever thread Combine publishes — @Published publishes synchronously on the setting thread (MainActor here). The sink closure: `[weak self] error in self?.scheduleBusRestart(...)` — scheduleBusRestart is @MainActor; the closure is called from MainActor context (since error is set from MainActor). But is the closure itself MainActor-isolated? The sink closure captures `self` (TSNetManager, MainActor). Swift 6 strict concurrency: `.sink { [weak self] error in ... self?.scheduleBusRestart(...) }` inside a MainActor method — closure is not annotated @MainActor, but it's created in MainActor context. With default actor isolation = MainActor, closures inherit... they'd be MainActor-isolated. OK, compiles (it does).

**But there's a subtle memory issue**: `startEventBus` is a nonisolated func (called from startTailscale Task off main). It does `await consumer.$error.sink { [weak self] error in self?.scheduleBusRestart(...) }` — the sink is a published-property subscription; it must be retained — stored in `busErrorWatcher` on MainActor. OK.

**busErrorWatcher generation race**: In `startEventBus`, the sink is created, then `await MainActor.run { guard expectedGeneration == ...; busErrorWatcher?.cancel(); busErrorWatcher = busObserver }`. Between sink creation and the MainActor.run, an error could fire and schedule a restart with the OLD busErrorWatcher still installed... the restart would use the new processor's generation. Probably fine.

### Bug candidate 11: **statusPollTask and `client.backendStatus()` failures are silently swallowed** (`try?`) — if URLSession is wedged post-suspension, the poll loops forever with `try? await Task.sleep(5s)` — the sleep is cancellable; on background `stopStatusPolling` cancels. OK. But if `backendStatus()` never returns (wedged URLSession without error), the poll task hangs INSIDE the await — `Task.sleep` never reached — and cancellation of the task does NOT cancel the URLSession call. The statusPollTask is cancelled on background; the cancelled task awaits `client.backendStatus()` forever → task stays alive (leak), holds the client. On foreground, `startStatusPolling` — statusPollTask is nil (set to nil in stopStatusPolling)... wait: `stopStatusPolling() { statusPollTask?.cancel(); statusPollTask = nil }`. Then foreground `startStatusPolling(generation:)` creates a new task. The OLD task is still awaiting backendStatus — leaked until that URLSession completes/times out (60s). It has `expectedGeneration` guard in its MainActor body — after it resumes, guard fails, exits. So a bounded leak with stale URLSession for up to 60s. And that stale URLSession is a new session created per request (`URLSession(configuration: sessionConfig)` — session not retained/invalidated!). **Each `backendStatus()` creates a brand-new URLSession and never calls `invalidateAndCancel` on it.** The session is deallocated when the function exits... URLSession dealloc → its tasks are cancelled by the OS eventually. This is generally OK but leaks resources under repeated polls? URLSession objects are deallocated after the request completes; fine, minor.

Wait, actually there's a subtle known iOS behavior: creating many URLSessions and letting them deallocate is discouraged (leaks until runloop drains), but not a hard bug.

### Bug candidate 12: **`MessageReader.stop()` is invoked from `MessageProcessor.cancel()` (caller's thread) and from `MessageReader`'s own workQueue in start** — comment says they fixed the race by making stop enqueue. OK.

### Bug candidate 13: **Startup failure → fatalError on a non-auth path**: `startTailscale` catch → `fatalError("Error setting up Tailscale: \(error)")`. If `loopback()` fails or `watchIPNBus` fails transiently at launch (e.g., slow disk, transient error), the whole app crashes. This is by design ("must never be silent") but a crash on any transient setup error is harsh — e.g., `startEventBus` failure → fatalError. Hmm, but watchIPNBus only creates a URLSession task — rarely fails. `loopback()` → C call — rarely fails. `setupNode` → tailscale_new/start — could fail if state dir locked? MEDIUM/LOW. Actually this is intentional design ("fail loudly"). I'll note it.

### Bug candidate 14: **`startInFlight` reset in `rebuildNodeAfterFailedResume`** — it sets `startInFlight = false` then `startTailscaleIfNeeded()` → sets true → Task startTailscale. If the new start also fails → fatalError. If user backgrounds during rebuild → `willEnterBackground` doesn't touch startInFlight — the in-flight start's `tailscaleUp` sees didEnterBackground → cancels processor, publishes proxy... and startInFlight stays true forever (never reset on success!). Look: `startTailscale` on success never resets `startInFlight = false`! It's only reset in the catch path and in rebuildNodeAfterFailedResume. So after a successful startup, `startInFlight` remains TRUE forever. Wait — that means `willEnterForeground` with node == nil never happens... and the guard `startInFlight` would block a SECOND startTailscaleIfNeeded call. Is that intended? The comment says "True while a start is in-flight or the node is up" — so it doubles as "node up" flag. OK, intended. But then if the node is up and something calls startTailscaleIfNeeded (foreground when node==nil? no), fine.

**But**: in `rebuildNodeAfterFailedResume`, `startInFlight = false` then startTailscaleIfNeeded → startTailscale → success → startInFlight stays true. Consistent.

**Edge**: `willEnterForeground` when `node == nil` but `startInFlight == true` (startup in progress during background) → `startTailscaleIfNeeded()` → guard: already in flight → skip → **no foreground recovery runs!** The startup task continues in background... it will complete (or not — URLSession wedged). If it completes while backgrounded, tailscaleUp cancels observers, publishes proxy. Then foreground: didEnterBackground true → willEnterForeground → node != nil now? If startup finished before foreground, node exists → recovery runs. If startup is STILL stuck (wedged URLSession on watchIPNBus — but watchIPNBus returns immediately after creating the task... the URLSession creation itself is fast). The slow part: `node.loopback()` and `setupNode`. Loopback is synchronous C — fast. So the window is tiny. OK.

### Bug candidate 15: **SocksLogProxy `waitingClients` unbounded when tsnet never comes up + WebKit keeps retrying** — bounded by WebKit's pool. And `handle` — when gate closed, client starts receiving? `client.start(queue: queue)` — receives data from WebKit (the SOCKS greeting) — but the client is never read → TCP backpressure. But NWConnection buffers incoming data in the kernel + NWConnection's internal buffer — if WebKit sends the greeting (small) it fits. If WebKit sends a large CONNECT (no, CONNECT is small). OK.

**Wait — a subtle deadlock**: while gate is closed, WebKit sends greeting; relay never reads → backpressure → WebKit's request held. When gate opens, `beginRelay` reads → greeting parsed → relayed. OK.

But **what if WebKit's connection